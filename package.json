{
  "name": "danflix",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "scripts"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "sync:sheet": "npm run sync --workspace=scripts",
    "resolve:scans": "npm run resolve-scans --workspace=scripts",
    "keep-alive": "npm run keep-alive --workspace=scripts",
    "refresh:tmdb": "npm run refresh-tmdb --workspace=scripts",
    "refresh:tmdb-title-index": "npm run refresh-tmdb-title-index --workspace=scripts"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
