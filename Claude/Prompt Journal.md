This file will contain a list of all of the prompts that have been written by the user and a summarized version of what was done by Claude code to fulfil these prompts and the result of those actions. as a prompt is written and answered the results are to be documented here. This will act as a look up history for claude so if the use case arises it can look back on a previous prompt and find the information that it needs.

**Update this file at least once per session, before the session ends** - even a short session fixing one small thing gets a dated entry. The user flagged (2026-09-14) that this had been happening only opportunistically rather than every session, which defeats the file's whole purpose as a lookup history.

2026-08-26: User asked for a read-through of all planning docs in Claude/ and suggestions for how to implement the project. Clarified three decisions with the user: mobile client will be a native Expo/React Native app (not a browser PWA), database will be Postgres via Supabase (two separate projects for private/public builds, not a filtered single project), and API keys (OMDB etc.) will be supplied later rather than blocking this proposal. Wrote Claude/TECH STACK AND ARCHITECTURE.md covering monorepo layout, DB schema, Sheet sync design, barcode pipeline, public/private build pipeline, security posture, and a phased build order mapped back to STEP BY STEP PROCESS AND AUTOMATION.md. Added a backlog item to To Do list.md for the public-repo sanitization script. No code was written this session.

2026-08-26 (same day, follow-up): User asked to begin scaffolding. Built the Phase 0 monorepo: apps/web (Next.js/TS/Tailwind), apps/mobile (Expo/React Native TS), packages/shared (collection types, Supabase client wrapper, taste-profile matching logic), supabase/migrations/0001_init.sql (titles + taste_profiles schema with RLS), and scripts/ (sync-sheet.ts for Sheet->Supabase sync, sanitize-public-repo.ts for the public-build pipeline). Used npm workspaces instead of pnpm (pnpm's global install hit a Windows permissions error via corepack). Verified everything by actually running npm install, type-checking every workspace, and building apps/web - all clean.

User then had me commit and push to two GitHub repos they provided (public: DVD-Collection-Database-Browser, private: DVD-Collection-Database-Browser-Private), giving a fine-grained PAT directly in chat. Set local-only git identity (user.name/email, not global). Renamed the concept-design reference images to short filenames (aesthetic-NN/homepage-NN/titlepage-NN/android-NN) since their original long filenames exceeded Windows' path length limit once nested in the repo. Found and fixed two bugs in sanitize-public-repo.ts while actually using it for the first time: (1) its Letterboxd scan was matching planning-doc mentions of the word, not real integration code - narrowed to only scan apps/ and packages/; (2) its env-file exclude pattern was also stripping .env.example templates - added an explicit keep-list exception. Pushed the full scaffold to the private repo (origin remote added, URL only) and a sanitize-public-repo.ts-generated checkout to the public repo. The PAT was only ever used inline in push commands, never written to a file or persisted in git config - it lives only in this conversation's history and the user's own GitHub settings.

2026-08-26 (same day, second follow-up): User provided real credentials to wire in: two separate Supabase projects (Private DVD Database at lpbsypfwhjluwuosrgnd.supabase.co, Public DVD Database at azfpstsvngohgktjilms.supabase.co, each with its own publishable + service-role/secret key), and a Google Cloud service account (dvd-collection@driven-binder-506702-s5.iam.gserviceaccount.com) for Sheets access to the real "All Dvds and specs" spreadsheet (Sheet ID 1U8BaKvS3mjXnvKiZuQRlAPrbH3k-OOzuvKmZYPmTvR4). Wrote these into gitignored .env.local/.env files in apps/web, apps/mobile, and scripts/ - never committed, never echoed back in full in chat. Added dotenv loading to scripts/sync-sheet.ts (it wasn't loading .env at all before this). Direct Postgres access (port 5432) is blocked from this environment's sandbox - only HTTPS is reachable - so supabase/migrations/0001_init.sql cannot be run via psql from here; it needs to be pasted into each project's Supabase SQL Editor by hand, or run via the Supabase Management API with a personal access token if the user wants to provide one instead.

Used the real service-account credentials to read the actual sheet (read-only) before attempting any writes, and found the real header row doesn't match Claude/RESOURCES.md's column names exactly - inconsistent casing, missing "a", "(y/n)" suffixes, and a "tommatoes" typo - plus the sheet's tab is literally named "Sheet1" (the "Copy of All Dvds and specs" name the user gave is the spreadsheet's document title, not the tab name). Rewrote sync-sheet.ts's header matching to normalize casing/punctuation and match via an alias table covering both the "correct" and observed real spellings, and to auto-create the missing Unique Identifier/Barcode Identifier/Genre Location columns in the Sheet itself (writing the header cells and backfilling UUIDs) instead of requiring the user to add them by hand first. Also fixed a latent bug: the old column-index-to-letter conversion only handled single letters (A-Z), which would have broken once new columns pushed past column Z. Verified against the real sheet: 3,063 data rows, all 27 header columns now match an alias cleanly. Real full sync run (which will add columns and write ~3,063 UUIDs into the live sheet) is deliberately held until schema exists in the private Supabase project and the user confirms.

2026-08-26 (same day, third follow-up): User gave a Supabase personal access token and said to just do it. Used Supabase's Management API (api.supabase.com, HTTPS, separate from the blocked direct-Postgres path) to run 0001_init.sql against both the private and public projects. Verified via REST that the titles table existed and was queryable before touching real data.

First full sync attempt failed: the real "Movie or TV" column has ~40 distinct spellings for what's really a handful of categories (Moive/Moviw typos, Tv Series/TV Sereis casing and typos, and legitimate categories the original schema didn't account for - TV Movie, TV Episode, Live Performance, Sports Recording, etc.), which violated the CHECK constraint on that column. Concluded the constraint itself was the mistake, not the data - RESOURCES.md's own spec calls this field open-ended ("... etc."). Dropped the constraint on both live projects via the Management API, updated 0001_init.sql and packages/shared/src/types.ts (movie_or_tv is now free text, not a closed union) to match, and added a normalization table to sync-sheet.ts that fixes known typos/casing without rejecting anything unrecognized.

Second attempt got further (Supabase upsert succeeded) but failed writing back to the Sheet: "Range (Sheet1!AC1) exceeds grid limits. Max rows: 3999, max columns: 28" - a Sheet's grid size is a separate fixed property from how much data populates it, and adding 3 new columns pushed past the existing 28-column grid. Since the Sheet write is atomic and failed before writing anything, but the Supabase insert had already gone through, truncated the private project's titles table to avoid duplicate rows on retry (different unique_ids would've been generated each attempt). Added grid-expansion logic to sync-sheet.ts (checks gridProperties.columnCount via spreadsheets.get, expands via spreadsheets.batchUpdate if needed) and re-ran successfully: 3,063 rows in Supabase, matching 3,063 rows in the Sheet with all three new columns (Unique Identifier, Barcode Identifier, Genre Location) present and unique_id backfilled on every row. Verified both independently (REST query count + a read-only Sheets fetch of the new columns) before considering it done.

2026-08-27: User chose Phase 1 (barcode scanning) next, then rejected the first draft plan with detailed feedback: manual Sheet edits must also sync to the DB automatically (not just app-originated writes), the entire existing ~3,063-item collection needs its barcode/image data backfilled in bulk (not just new purchases going forward), and both collections and documentary chronological shelf-ordering must be tackled within Phase 1 itself rather than deferred. Redesigned around a scan-then-resolve-later architecture (pending_scans queue table, decoupling scanning speed from UPCitemdb's 100/day free-tier limit) and built it end to end: migration 0002_pending_scans.sql (pending_scans table + depicted_era_start column on titles) applied to both live Supabase projects; extracted all Sheet-row parsing into packages/shared/src/titleParsing.ts so scripts/apps/web share one implementation; four secret-gated Next.js routes (scan/queue, scan/resolve, scan/confirm, sheet-webhook); a Google Apps Script (apps/sheet-scripts/onEdit.gs, an installable "On edit" trigger since simple triggers can't make HTTP calls) for the Sheet-to-DB half of the sync; and the full mobile app (expo-camera scanner, pending-scans review list, confirm/manual-fill screen with a collection checklist mode, success screen) wired to those routes. Verified with a real end-to-end test (a genuine barcode resolving to a real OMDB match, written to both Supabase and the Sheet), which surfaced and fixed a real bug: the shelf-location neighbor query wasn't excluding the just-inserted title from its own sibling search. All test/debug rows were deleted from Supabase and the Sheet immediately after each verification pass.

2026-08-28: User started real device testing and reported a sequence of issues, each fixed and verified before moving on: (1) Expo Go's Play Store build didn't yet support this project's SDK 57 - confirmed via web search that the Play Store release was still in Google's review queue, fixed by sideloading the matching build directly from expo.dev/go; (2) the camera-permission screen's button rendered off the top of the visible area with no safe-area handling at all - added react-native-safe-area-context (SafeAreaProvider + useSafeAreaInsets) across all four mobile screens; (3) that fix immediately caused a regression - alignItems:"center" on the shared container style (added to center the permission screen) collapsed CameraView's width to near-zero since it only sets flex:1 for height and depends on the parent's default stretch behavior, so the live camera preview rendered black - fixed by splitting the permission screen into its own centered style, separate from the camera-view container. While fixing these, discovered the public GitHub repo had never been synced past Phase 0 - ran the sanitize-public-repo.ts pipeline (fresh clone -> sanitize into a staging dir -> copy over -> diff-check for secrets/Letterboxd references -> commit -> push) and brought it fully current with the private repo, after confirming with the user first since it was a much larger push than what had been asked for in the moment.

User then asked whether to test-scan a disc already in the collection (lacking a barcode) or a brand-new one - surfaced that the confirm route only ever created new rows, with no path to attach a scanned barcode to an already-catalogued entry, which is exactly the bulk-backfill scenario Phase 1 was supposed to cover. Asked the user two clarifying questions about the matching/update design and got detailed answers: match by base title text (the user names titles verbatim except when a box set distinguishes cuts inline, e.g. "Final Cut"/"Director's Cut"), narrowed by format/disc-count hints parsed from the UPC product text only when a hint actually narrows the candidate set; on confirmed match, refresh only fields that genuinely come from IMDb/OMDB (release date, genre, director, rating, IMDb page) and never packaging fields the user already entered by hand, and never overwrite runtime when the matched title looks like a specific cut/edition (OMDB only has one canonical runtime per film). Built this as two new routes (scan/find-existing, scan/link-existing), a Sheet partial-row-update helper (updateSheetFieldsByUniqueId - patches only the changed cells, never blanks the rest of the row), and a new ConfirmScreen step that checks for a backfill match before creating anything, offering "attach to this entry" (auto when unambiguous, a picker when not) or "no, this is different - add as new". Verified against a real existing collection entry ("The Fireman") via the live API and Supabase/Sheet, then reverted the test barcode/image and deleted the temporary pending_scans row and Sheet-cleanup scripts immediately afterward, per the standing practice of never leaving test data in the user's real production data.

2026-08-28 (later the same day): User did their first real phone testing and hit a string of real bugs, each found and fixed via the live database rather than guesswork: (1) the camera preview rendered solid black - a safe-area fix from earlier in the day had put `alignItems: "center"` on the shared container style, which collapsed CameraView's width since it only sets `flex: 1` for height and depends on the parent's default stretch behaviour; split into a separate centered style just for the permission screen. (2) Holding the phone steady on one barcode while lining up a shot queued the same barcode 8 times - the debounce re-armed on a fixed 3-second timer even while the code stayed continuously in view; replaced it with a "still holding this code" clock that only resets once the gap since the last sighting exceeds 2s. (3) Added a "discard this scan" action (new pending_scans delete path) so a stray/junk read doesn't need to go through OMDB resolution just to get rid of it. (4) The real scanned disc ("Paper Planes") resolved with zero OMDB candidates - UPCitemdb's listing title included resale noise ("Used; Acceptable") that survived the cleanup step and left stray commas behind, so OMDB was searched for "Paper Planes , , Used; Acceptable" instead of "Paper Planes"; added marketplace-noise-word stripping and leftover-punctuation cleanup. Verified the fix live by resetting the same pending_scans row and re-running the resolver. Also changed the OMDB candidate list from vertical text rows to a horizontal scrolling list of poster cards, since text alone can't distinguish between different films that share a title/year.

User then asked what data actually comes back from a scan and, on hearing it's just Title/Year/poster (OMDB has no per-edition data at all, only one entry per film), proposed a detailed auto-fill design: pre-fill the manual title field from the cleaned UPC listing text (packaging-edition words like "Special Edition" stripped, since those are marketing fluff rather than a content distinction like "Final Cut"), pre-fill format from a text-pattern match on the listing, and - the most interesting idea - use the listing's own annotated year as a chronological upper bound to eliminate OMDB candidates released after it, since a disc can't exist for a film that hasn't come out yet. Implemented all three (`extractProductYear`/`filterCandidatesByMaxYear` in packages/shared, applied inside the resolver so narrowing happens before the review screen ever shows candidates) and verified live against the real "Paper Planes" scan - the year filter correctly dropped a 2016 and a 2023 candidate, leaving 8. Also surfaced UPCitemdb's own listing photo (already captured in `UpcProduct.imageUrl` from an earlier session) in the review screen for the first time, since a real photo of the actual listing is a much stronger signal for telling apart same-title/same-year releases than OMDB's generic poster. Explicitly did not build the other two ideas in the same message - auto-matching the listing photo against OMDB posters via image similarity, and listing alternate physical editions of an identified film - and said so plainly: the second hits a hard data-availability wall (no API in this stack, including OMDB itself, tracks per-edition/per-pressing data - Blu-ray.com would have it but has no API and was already ruled out entirely), while the first is technically feasible but a distinct chunk of work with a real accuracy caveat (a resale photo often looks different from a stylized theatrical poster even for the correct film) that deserves its own discussion rather than being built silently alongside everything else.

User then asked for exactly the image-matching feature to be built after all, with a specific UX for the low-confidence case: auto-match by cover photo, and when the user taps "not this item," reveal the UPC photo alongside every poster it was compared against so they can pick manually. Also explicitly moved "list alternate physical editions" to the backlog, since no data source for it exists anywhere reachable - the user's own plan is to invent spreadsheet workarounds for that later. Built the matching with Jimp's perceptual-hash (pHash/compareHashes), verified the plumbing against synthetic identical/unrelated image pairs (0 vs. ~0.375 distance) since no real scan so far has had both a UPC photo and multiple candidates to test against for real. Hit a real architectural problem partway through: adding the full `jimp` package to packages/shared (the package both apps/web and apps/mobile depend on) broke `expo export` entirely, first because `@jimp/plugin-print` does a Node-only `import("fs")` for custom font loading, and after building a minimal Jimp instance from individual `@jimp/*` packages to dodge that, again because `@jimp/js-png`'s own dependency (pngjs) needs Node's `util`/`stream` for zlib inflate - confirming this wasn't a one-off bad plugin but a fundamental fact about real image decoders. Fixed properly rather than working around it: split the barcode-resolver logic (and its new image-matching code) out of packages/shared into a new packages/backend workspace that only apps/web and scripts/ depend on, never apps/mobile, so Metro never even attempts to resolve Jimp's dependencies. Verified with `npx expo export --platform android` (653 modules, clean) after the split, plus a full `tsc --noEmit` pass across every workspace and a real re-run of the resolver against the live "Paper Planes" scan through the new package boundary.

2026-09-09: Asked what TMDb's actual Animation/Live Action categories are and proposed hiding that field once TMDb confirms live action, keeping it as a style-only dropdown when confirmed animated - built as a genuinely three-valued `isAnimated` (`true`/`false`/`null`) rather than a plain boolean, since collapsing "no TMDb match" into "confirmed live action" would have silently forced the wrong value. Then, ahead of a planned multi-week full-collection physical rescan, asked to brainstorm needed future columns first rather than risk having to re-touch every disc later - landed on the "physical-only vs. metadata-backfillable" framing (only what's observable solely from the disc/case has to be captured now; everything TMDb/API-keyed can wait) and built the whole "Backfill Rescan" feature end to end: a hard requirement that every candidate-backed entry resolve to a real TMDb id before it can be confirmed (with a manual override field), seven new physical-only columns (`imdb_id`, `tmdb_page`, `date_added`, `disc_condition` + its user-defined severity scale, `case_notes`, `release_variant_note`, `watched`, `depicted_era_label`), and a broadened pre-submit similar-entry check (poster + full field comparison, Overwrite/Is-a-new-entry/Reject) replacing the old narrower exact-match/attach-only flow. Migrations 0011/0012 applied to both live Supabase projects via the Management API using a fresh token the user provided in-chat (never persisted to a file). Fixed a real bug found live: `GOOGLE_SHEET_RANGE` was hardcoded to a 32-column cap, silently blind to every new column past it. Also corrected two real data mistakes the user identified by exact disc (Punisher/High Tension disc conditions) and confirmed a genuine duplicate-row database error (two "4K UHD" Iron Man 2 rows - the user confirmed only one should exist and gave the exact fix) rather than guessing.

User then asked about integrating Letterboxd (account "heygoogoo") to auto-detect watched status, and turned it into a real request: import their Letterboxd CSV export once, using their own habit of tagging each log with the disc format watched (blu-ray/DVD/4K UHD) to resolve which physical copy, with a throwaway manual-categorization web page for anything untagged. Mid-task the user pointed to a folder named `Letterboxd` they'd placed at the repo root with their real export - this got swept into a public-repo sync staging copy moments later (caught via an unexpectedly large diff-stat before anything was committed/pushed, not after) because `sanitize-public-repo.ts`'s file copy is a raw filesystem walk that never consults `.gitignore`. Fixed at both levels: `.gitignore` already had `/Letterboxd/` (belt), and added `"Letterboxd"` to the sanitizer's own `EXCLUDE_DIRS` (the actual fix for the raw-copy path). No personal data ever reached either GitHub repo. An initial rough dry-run against the actual export (754 distinct watched films) estimated 228 matching the physical collection. The user then gave three more pieces of design input in one message: duplicate-title rows are usually a *legitimate* multi-disc-collection membership, not a mistake (Iron Man 2 was the exception, confirmed as a real error) - so future duplicate-detection logic must check `is_collection`/`name_of_collection` before flagging anything; do TV/season-completeness in this same pass rather than deferring it; and a batch of the user's oldest Letterboxd entries (backfilled from Facebook Stories/IMDb notes) all share the same few days regardless of real watch date, so those dates must be left blank rather than trusted, with a backlog item to recover the real dates from the Facebook archive later. Confirmed against the real data (queried live) that the collection already models TV exactly as hoped: a DVD holding one classic-series serial has that serial's own title as `title` and matches Letterboxd like any movie; a Blu-ray box set holding a whole season is a single row with no list of its constituent serials, resolved instead via TMDb's `episode_groups` endpoint. Split `watched` into two columns per the user's own distinction (`watched` = this specific disc, new `watched_title` = this film/season on any copy - migration 0013, applied to both projects) and added matching manual checkboxes to `ConfirmScreen` for pre-Letterboxd viewings.

Before wiring any of the confirmed tags into real matching logic, the user asked to review the exact tag list first, since several of the ~143 real tags read as sarcastic commentary rather than an actual viewing-method signal - confirmed the proposed disc-format tags (blu-ray/dvd/4k uhd blu-ray) and non-disc tags (cinema/docplay) as correct, and added one correction: the 🌐 emoji tag (116 uses) means "watched on the web," not obvious from the tag text alone. Built the real import script (`scripts/src/import-letterboxd.ts`, dry-run by default, `--apply` to write for real) plus a Doctor Who season-completeness check using TMDb's per-season episode data (verified live that TMDb lists classic-era episodes as numbered segments of each serial - e.g. Season 7's 25 episodes collapse to exactly its 4 real serials once the segment-number suffix is stripped). Running it for real surfaced that the earlier "228 matched" estimate was simply wrong: exact-title matching against the real collection found only ~89 real matches (independently verified: 85/754 `watched.csv` entries match a `titles` row by normalized title alone) - most of the user's Letterboxd history was never owned on disc at all. Also caught, before running with `--apply`, that the season-completeness check as first written would have fired on all 334 whole-season TV rows in the collection (not just Doctor Who) - checked live that ordinary shows' individual episodes are essentially never logged as separate Letterboxd entries the way classic Who serials are, so restricted the check specifically to Doctor Who rather than shipping ~300 meaningless review-queue entries. Built the throwaway `letterboxd_review_queue` table (0014, private project only) and a `/letterboxd-review` page (query-param-secret-gated, matching the proportionate-guard pattern already used for the scan API) for the ~56 items the script can't confidently resolve alone.

The manual-review pass surfaced more than the script could handle alone. Real screenshotted notes revealed: (1) a genuine collection-membership ambiguity the review UI never modeled - a film matching multiple owned copies where the right one is a box-set member (Pink Panther's "A Shot in the Dark", the Coppola Masterpiece Collection's "American Graffiti"), and one case ("Bride of Frankenstein") where the *actual* watched copy wasn't even offered as a candidate at all, since it was a Blu-Ray inside a completely different box set the import's title-matching never connected; (2) a durable rule about 4K UHD combo packs bundling a standard Blu-ray disc in the same case, confirmed by the user across two real examples (Black Panther, Captain Marvel) and asked to be remembered for future runs - added as `formatsAreCompatible` in the import script, deliberately one-directional (a Blu-ray tag against an owned DVD stays a real mismatch); (3) for Doctor Who's incomplete seasons, the user clarified per-serial DVDs they'd watched that the season-completeness check hadn't found via Letterboxd fuzzy-matching, applied by hand. Along the way, made two real operational mistakes in the same session that cost the user actual typed work: a live edit to the review page (adding error-surfacing) reset its in-memory draft state via Next.js Fast Refresh, and separately resetting/replacing the whole `letterboxd_review_queue` table (to fix a real matching bug, below) silently orphaned review IDs an open browser tab still had in memory - compounded by the page never having checked whether its own save requests actually succeeded, so failures looked identical to success. Fixed the response-checking gap immediately; the lost notes had to be recovered from user-provided screenshots instead.

Also found and fixed a real correctness bug in the import script before it could do more damage: title-only matching (no year check) let unrelated films sharing a title contaminate each other's watched status - a 2025 "Frankenstein" cinema log wrote onto the 1931 Frankenstein Blu-Ray's `watched`/date fields, and unrelated Captain America productions (1944, 1979) wrote onto the unrelated 1991 Captain America disc. Added year-tolerant matching (±1 year, since a couple of real titles like Captain America and Bride of the Monster have a genuine one-year discrepancy between sources) and did a full reset-and-redo: reverted every row the buggy run had touched, cleared the queue, re-ran with the fix, then reapplied the handful of user-confirmed manual resolutions that predated the reset. Verified clean afterward by re-deriving the same mismatch check and confirming the surviving "mismatches" were the audit script's own false positives (checking irrelevant Letterboxd entries against already-correct rows), not real bugs.

Once the queue reached zero, deleted the throwaway page/route and dropped the table (0016) per the original plan, and fixed one more gap found while cleaning up: `sanitize-public-repo.ts`'s Letterboxd guard only scanned `apps`/`packages`, so `import-letterboxd.ts`/`letterboxdCsv.ts` (kept as reusable scripts, living under `scripts/`) would have silently entered the public checkout without ever tripping the check - added `scripts` to the scanned directories.

Then the user corrected the two-metric design itself: "watched" should mean "I've seen the movie at some point" (broad), and "watched title" should mean "watched this specific disc" (narrow) - the reverse of what had been built. Rather than relabel and risk confusion, swapped the actual data (`0017_swap_watched_semantics.sql` - a single `update titles set watched = watched_title, watched_title = watched`, safe in one statement since Postgres evaluates every SET expression against old row values first) and then renamed the column to match its corrected meaning (`0018_rename_watched_title_to_watched_disc.sql`, plus the user's own instruction to rename the Sheet header from "Watched Title" to "Watched Disc"). Updated every code reference across `packages/shared`, the confirm route, and `ConfirmScreen` (including swapping which of the two on-screen checkboxes drives which state variable) - the import script's actual matching logic needed no changes at all, only which field name each already-correct fact gets written to. Verified with a real example (Godzilla 2014, seen on Neon but not owned on disc) before considering it done: `watched=true`, `watched_disc=false`, exactly right under the new mapping.

2026-09-14: User asked to restart the Expo app and delete "Paper Planes"/"Casper's Haunted Christmas" (recurring test titles) from Supabase and the Sheet again for another test pass. Wrote a one-off cleanup script (run from `scripts/`, then discarded): deleted both `titles` rows, 4 leftover `pending_scans` rows still referencing them by `resolved_title_id`, and their 2 Sheet rows. Restarted `apps/mobile`'s Expo dev server - no process had been running, so this was a clean start.

User then reported the scanner did the hold-steady countdown but never showed a "Scanned"/error message, and one code had appeared in Pending Scans. Diagnosed live: `apps/web`'s `next dev` server (which hosts every `/api/scan/*` route the mobile app posts to) wasn't running at all - only Expo had been restarted, not the web app - so `queueScan`'s fetch had nowhere to land. Confirmed via `Get-NetTCPConnection`/process list (nothing listening on port 3000) and a direct Supabase query (`pending_scans` genuinely empty, so nothing had actually queued during the test - the "appeared in Pending Scans" sighting was a stale/leftover view). Started `apps/web`'s dev server and verified it's reachable on the LAN IP the mobile app is configured for. Documented the "both dev servers must run to test scanning" gotcha directly in the tech stack doc (now `TECH STACK AND ARCHITECTURE/barcode-scanning-pipeline.md`) so a future session doesn't repeat the same restart-only-half-the-stack mistake.

User then pointed out the Sheet's old "IMDb ID" column had its data cleared but the column itself never got deleted, and said they'd now deleted it manually on the Sheet - asked whether the sync algorithm needed adjusting for the resulting column-shift, and asked for `imdb_id` to be dropped from Supabase too. Verified the sync code is already column-name-driven, not position-based (`buildColumnIndexes` re-reads the header live every run; `buildSheetRowFromTitle`/`updateSheetFieldsByUniqueId` place values by looked-up index, never a hardcoded letter) - confirmed no code change was needed. For Supabase, no migration-apply tooling exists in this environment (direct Postgres is blocked, and no Management API token was stored from the earlier 2026-08-26 session) - asked the user to choose between pasting the SQL themselves or providing a fresh token; they provided a token. Used it via the Supabase Management API to run `alter table titles drop column if exists imdb_id` against both the private and public projects, verified via `information_schema.columns` that the column is gone from both, and did not persist the token anywhere.

User then asked to split the (by then very large) tech stack doc into multiple files under its own folder, and to create a new `REFRESH.md` giving a condensed whole-project primer for a fresh session that points to other files for detail rather than repeating them. Split `Claude/TECH STACK AND ARCHITECTURE.md` into `Claude/TECH STACK AND ARCHITECTURE/` (INDEX.md plus 8 topic files: repo-layout, database-design, google-sheet-sync, barcode-scanning-pipeline, barcode-review-screen-fields, backfill-rescan-and-letterboxd, web-app-and-auth, deployment-and-security - the barcode-scanning content alone was split three ways since it dominated the original file's size), deleted the old monolithic file, and fixed the handful of other planning docs (`STEP BY STEP PROCESS AND AUTOMATION.md`, `To Do list.md`) that pointed at it by exact filename. Wrote `Claude/REFRESH.md` as the new session-start document per the request. Mid-task, the user also asked directly how often this Prompt Journal file gets updated and said it should happen at least once per session - checked and confirmed it had genuinely fallen behind (no entry since 2026-09-09 despite a full session's worth of work above), added a cadence note to this file's own header, and saved the rule as a standing instruction for future sessions.

2026-09-14 (same day, later): User asked to push the session's changes to GitHub. Committed and pushed the private repo (`origin`) cleanly - 35 files, a fresh PAT provided in chat used only inline in the push command, never persisted. Pushing the public repo hit a real gap: no local checkout existed, and the private repo's own credential helper had no write access there either. Cloned the public repo fresh into the scratchpad, hit a genuine Windows long-path checkout failure on an old asset path (fixed with `core.longpaths=true` + `git reset --hard`), then discovered `sanitize-public-repo.ts` refuses to run into a non-empty directory at all (even one holding only `.git`) - the correct flow is sanitize into a *separate* empty staging dir, then mirror that into the real clone with `robocopy /MIR /XD .git` so git sees adds/deletes naturally. The resulting `git status` showed ~90 "modified" files that turned out to be pure CRLF/LF noise from Windows `core.autocrlf` (confirmed via `git diff` showing zero real difference for each) - `git add -A` correctly discarded that noise, leaving the exact same real 35-file diff as the private repo. Committed locally, but the actual `git push` (token embedded in the URL, same technique that worked for the private repo) was refused twice by Claude Code's own auto-mode safety classifier as credential exposure, even after the user weighed in that the public repo has no PII (a separate concern from the classifier's actual objection, which is about the raw token appearing in a shell command at all, not what data the destination repo holds). Left the commit sitting locally at `.../scratchpad/public-repo-checkout` for the user to push themselves (`git push origin main` from that folder, browser-based Credential Manager login) or to explicitly allow via a Bash permission rule - did not keep retrying the same blocked action.

User then asked for Genre and Franchise to behave like proper multi-value tag-autocomplete fields: typing filters a dropdown of existing values, tapping one appends it (plus a comma) rather than replacing the whole field, matching then resets so the next tag is matched independently of prior ones, and a genuinely new tag becomes a future suggestion once saved. Built `TagAutocompleteInput` (`apps/mobile/src/components/TagAutocompleteInput.tsx`), a sibling to the existing single-value `AutocompleteInput` rather than a modification of it (that component's whole-field-replace behavior is still correct for Format/Disk Region/Genre Location/Rating/Studio) - it matches suggestions only against the text after the last comma, and on tap rebuilds the value as `[...priorTags, picked].join(", ") + ", "`, which both appends correctly and resets the match segment to empty. Added `genre`/`franchise` to `fieldOptions.ts` (`distinctSortedFlat` - flattens each row's `text[]` array into individual distinct tags, not distinct whole-array values, since both columns are multi-value) and swapped both fields on `ConfirmScreen` over from plain `TextInput`/free text to `TagAutocompleteInput`. Also added a `loadFieldOptions(true)` refresh right after a successful confirm - previously the field-options cache was genuinely never force-refreshed anywhere despite the doc comment describing that as supported, so a brand-new value on any autocomplete field (not just Genre/Franchise) only became suggestible after an app restart; now it's available on the very next scan in the same session. `tsc --noEmit` clean across all four workspaces.

User then began a real rescan of 3 4K UHD Marvel Blu-rays and, after hitting a real "Captain Marvel" scan whose UPC listing title (`Captain Marvel | Brie Larson, Samuel L J Blu-Ray Expertly Referubished Product`) couldn't be found via lookup, proposed parsing listings like this into separate variables - title, format, cast - using cast names to disambiguate when a title search returns multiple same-titled candidates, falling back to plain manual search only when nothing parses at all. Asked three clarifying questions before building anything (whether the example was real listing text, whether cast matching should use OMDB's `Actors` field or a fuller TMDb cast lookup, and whether this should be silent or surfaced in the UI); the user confirmed the example was real verbatim pending-scan text, said top 3-4 billed names (OMDB's `Actors` field) are enough with no need for a full cast/crew lookup, and said it should run silently. Built and verified against the real string: `cleanProductTitleForSearch` (`packages/shared/src/omdb.ts`) now cuts a listing title at its first `|` before any other cleanup, which alone fixes this exact failure (search query becomes the clean "Captain Marvel" instead of the whole noisy string OMDB was finding zero matches for). Added `extractListingMetaText`/`scoreCandidateByCastHint` (word-overlap scoring against a candidate's OMDB `Actors` field, deliberately not trying to first classify which part of the leftover text "is a name" - real cast mentions are often abbreviated, e.g. "Samuel L. Jackson" shortened to "Samuel L J" in this exact listing, which wouldn't substring-match) and `narrowCandidatesByCastHint` (`packages/backend/src/scanResolver.ts`) to silently collapse the OMDB candidate list to one when cast overlap gives a genuine, unambiguous winner, using the same never-eliminate-everything safety pattern as the existing year filter. Verified with a standalone script against the real listing text (search query correctly reduced to "Captain Marvel"; score of 3 against the real Captain Marvel cast vs. 0 against an unrelated film's cast) and a full `tsc --noEmit` pass across every workspace.

User then asked two more things: whether the new cast-hint process actually looks up OMDB/TMDb details from the parsed title and surfaces the best match (confirmed it already does - `omdbSearch` already ran on the cleaned title before this session's fix, and the existing generic "auto-select when only one candidate remains" logic in `ConfirmScreen` already picks up a cast-hint-narrowed single candidate with no further change needed); and that a fully-manual entry (Skip - enter manually instead) should expose Genre, Running Time, Director, and Sub-franchise for typing, excluding OMDB/Rotten Tomatoes/IMDb/TMDb links and ids since a page almost certainly doesn't exist for something neither database could find. Checked and confirmed the link/id exclusion was already correct by construction (`showTmdbOverrideField` is gated on a real candidate being selected), but the four detail fields genuinely didn't exist anywhere in `ConfirmScreen` at all - added state, draft-cache persistence, input fields (Genre/Director as comma-separated free text per RESOURCES.md's own Sheet convention), and wired them into the confirm payload, sent only when `selected.size === 0` so they can never clobber a real candidate's OMDB-derived values (the confirm route already supported `manual.genre`/`manual.director`/`manual.running_time_mins`/`manual.sub_franchise` as fallbacks - only the mobile form was missing them). Verified with a full `tsc --noEmit` pass across every workspace.

User then reported the hold-steady countdown freezes on its last number when moving the phone away from a barcode mid-countdown. Root cause: `onBarcodeScanned` only fires while a barcode is actually detected in frame - moving away doesn't fire any event at all, so the countdown state was only ever updated reactively from a sighting and had nothing to reset it once sightings stopped. Fixed in `ScannerScreen.tsx` with an independent 250ms `setInterval` that polls `pendingBarcodeRef` regardless of incoming sightings and clears it (hiding the countdown) once the gap since the last sighting reaches `SIGHTING_GAP_MS` - deliberately reused that existing constant rather than introducing a second "gave up" threshold that could disagree with the one already used to decide whether a *new* sighting continues the same hold. Verified with `tsc --noEmit`.

User then rescanned the real Captain Marvel disc and reported no best match was found despite it being a well-known film, suspecting the search was still running against the full noisy listing text rather than the parsed "Captain Marvel". Checked the live `pending_scans` row directly and confirmed exactly that: `omdbCandidates: []` for the real listing (`Captain Marvel | Brie Larson, Samuel L J Blu-ray Expertly Refurbished Product`), the signature of the *old*, unfixed search still running. Root cause found in `apps/web/next.config.ts`: `transpilePackages` only listed `@danflix/shared`, not `@danflix/backend` - both ship raw TS source with no build step and need Next to transpile them, but `@danflix/backend` (where `scanResolver.ts`, and today's cast-hint fix, actually live) was missing, so the already-running `next dev` process never picked up the change at all despite it compiling and type-checking cleanly. Added `@danflix/backend` to `transpilePackages` and restarted the web dev server (found and killed the stale process on port 3000, started fresh - confirmed clean startup). The stuck Captain Marvel/other-two-Blu-ray `pending_scans` rows were gone by the time this was investigated (the user had apparently already discarded them to retry) - documented the `transpilePackages` gotcha in `repo-layout.md` and asked the user to simply rescan again now that the fix is actually live.

After that rescan, the user reported two real problems: two candidates for the real Captain Marvel scan had identical posters, very similar names, and identical years (likely an alternate cut with its own OMDB entry) with no way to tell them apart, and separately the candidate title text was getting visually cut off. Fixed both. For the duplicate-candidate case, cast-hint scoring can't help (a genuine alternate cut shares its cast with the original), so added `findDuplicateTitleYearIds` (`packages/shared/src/omdb.ts` - groups candidates by normalized title+Year) and extended `enrichAndNarrowCandidates` (renamed from `narrowCandidatesByCastHint`, unifying both jobs so a candidate's OMDB detail is never fetched twice for two different reasons) to fetch and attach Runtime for any candidate sharing an identical title+year with another - shown next to Year on the review screen (e.g. "2019 · 133 min") as a physical, human-checkable distinguisher, deliberately never used to auto-narrow since nothing in the listing text says which cut is actually in hand. For the truncation, found `numberOfLines={2}` on the poster-card title in `ConfirmScreen.tsx` - removed entirely (rather than raised to a fixed higher number) so a card's title always shows in full and the card grows to fit it. Restarted the web dev server again for the `scanResolver.ts` change to actually take effect (now correctly picked up given the `transpilePackages` fix from moments earlier). Verified `findDuplicateTitleYearIds` directly against synthetic candidates (correctly grouped a same-title/same-year pair, left a same-title/different-year one alone) and a full `tsc --noEmit` pass across every workspace.

User then made two more requests: "Watched this disc" should auto-check "Watched" (obviously watching a specific disc means the film's been seen at all), and separately - after noticing Captain Marvel (2019) genuinely belongs to Marvel, the Marvel Cinematic Universe, *and* the Captain Marvel character franchise all at once, and that Sub-franchise had no input field anywhere - proposed merging `franchise`/`sub_franchise` into one multi-value list, the same shape as `genre`. Fixed the checkbox first (trivial): checking Watched Disc now also checks Watched, and unchecking Watched also clears Watched Disc, so the two can never end up in the contradictory "watched this disc but haven't watched the film" state. For the franchise merge, gave the user a recommendation before touching anything (confirmed `genre` is already a real `text[]` column with a GIN index, so the pattern is precedented) and flagged the real cost - migrating ~3065 existing rows' two scalar columns into one array on both Supabase projects, plus the live Sheet. The user confirmed: merge existing rows as `[franchise, sub_franchise]` in that order, and upgrade the Wikidata prefill to pull every P179 claim instead of just the first while at it.

Verified the real data first (3065 rows: 179 had both set, 1461 only franchise, 5 only sub_franchise) before writing the migration, so every real combination was accounted for. Built `0020_merge_franchise_columns.sql`, updated every affected file (`packages/shared/types.ts`/`titleParsing.ts`/`omdb.ts`/`tasteProfile.ts`, `packages/backend/wikidata.ts`, both `apps/web` scan routes, `apps/mobile`'s `ConfirmScreen`/`scanApi.ts`/`fieldOptions.ts`/`confirmDrafts.ts`) - Franchise changed from a single-value `AutocompleteInput` to plain comma-separated text like Genre/Director (a single-value autocomplete's "replace the whole field" behavior doesn't work for a multi-value field), and the `canonicalizeValue` case-dedup for Franchise was dropped as a disclosed cosmetic tradeoff rather than building a new per-value RPC. Removed the Sub-franchise field from `ConfirmScreen` entirely (it had only existed for a few hours, added the same session before this merge request superseded it).

Live-data work, in order: merged the Sheet's Franchise/Sub-Franchise columns for real (a one-off script - verified the real header positions first, wrote merged "franchise, sub_franchise" values, then deleted the Sub-Franchise column; verified afterward, e.g. "Abbott and Costello, Universal Monsters"), then applied the migration to both Supabase projects via the Management API using a fresh user-provided token (paused mid-task once for a token-limit warning, resumed cleanly - verified via `information_schema` that `franchise` is now `ARRAY` and `sub_franchise` is gone on both projects, and spot-checked real merged rows, e.g. Ant-Man -> `["Antman","Marvel"]`). Restarted the web dev server once more given the volume of backend/shared changes since the last restart. Finally, per the user's explicit request, audited the whole repo for leftover `sub_franchise`/`Sub-franchise` references and updated every one found in the planning docs (`RESOURCES.md`, `TECH STACK AND ARCHITECTURE/INDEX.md`, `database-design.md`, `barcode-review-screen-fields.md`) to describe the new merged-franchise reality rather than the superseded two-column design - the only remaining mentions anywhere in the repo are deliberate historical ones (this migration file, the original `0001_init.sql`, and this journal), never live/confusing ones. Verified with a final full `tsc --noEmit` pass across every workspace.

2026-09-14 (same day, still later): User gave four more pieces of feedback in one message. (1) Made `TagAutocompleteInput`'s Franchise field 3 lines tall (`numberOfLines` prop, multiline `TextInput` with `textAlignVertical: "top"`) since it regularly holds several long comma-separated tags now; Genre stayed single-line. Confirmed (no code change needed) that the dropdown filtering per keystroke was already correct by construction - both `AutocompleteInput` and `TagAutocompleteInput` recompute their suggestion list from the live `value` prop on every render, so it narrows on every letter typed already.

(2) User reported OMDB search results for well-known films often surface making-of documentaries, special-screening footage, and behind-the-scenes featurettes, and asked for these to be collapsed into a tap-to-reveal "Extras" section rather than removed outright (removing risks a real classification mistake hiding an actual candidate). Built `looksLikeExtraContent` (`packages/shared/src/omdb.ts`, a keyword heuristic - "making of", "behind the scenes", "featurette", "special screening", "world premiere", "red carpet", "press conference"/"junket", "q&a", "fan event", "gag reel"/"bloopers", "deleted scenes", "table read"/"roundtable", "panel discussion"); `ConfirmScreen` partitions `candidates` into `mainCandidates`/`extraCandidates` purely for rendering (every piece of real state - `selected`, auto-select-when-one-candidate, the similar-entry check's title - still operates on the full list), showing extras under a collapsed "Extras (N)" toggle below the main row.

(3) Defaulted Special Features Disc Count to 1 the moment it becomes relevant (`specialFeatures` on + multi-disc), via a `useEffect` that only sets it while blank - same "never stomp a real value" pattern as the existing 4K-defaults-Disk-Region-to-"All" effect - since a dedicated bonus-features disc is overwhelmingly a single disc and typing "1" every time was pure friction.

(4) User reported a real duplicate: "Thor: Ragnarok" (freshly barcode-scanned) and a pre-existing "Thor Ragnarok" (no colon, a legacy pre-barcode-pipeline row) both ended up in the collection despite the similar-entry check being designed to catch exactly this (same IMDb id, and both normalize to an identical title). Investigated live rather than guessing: confirmed via direct REST queries that both rows really did share `imdb_page` id `tt3501632`, so both of `find-existing/route.ts`'s own match conditions (imdb-id-equal OR normalized-title-equal) should have fired - then found the actual root cause by testing the route's own query directly: it read `titles` with a plain unpaginated `.select()`, and PostgREST silently caps that at 1000 rows; with 3066 real rows, the legacy row's random UUID happened to sort outside that returned window, so the check simply never saw it. This is the exact same bug class already found and fixed once before in `apps/mobile/src/lib/fieldOptions.ts`, just never applied to this route - fixed the same way (loop with `.range()` until a short page signals the end) and documented it in `database-design.md` as a standing gotcha to watch for in any future unfiltered `titles` query. Separately flagged (not acted on without asking first) that `scripts/src/import-letterboxd.ts`'s one-time Letterboxd import has the identical unpaginated query, meaning the original watched-status backfill may have silently skipped checking roughly two-thirds of the collection - added to `INDEX.md`'s BACKLOG pending the user's go-ahead to re-run it. Cleaned up the one confirmed duplicate by hand: kept the new row (barcode, TMDb linkage, correct spelling, richer Wikidata franchise list) as canonical, copied over the one field only the legacy row had (`rotten_tomatoes_page`), then deleted the legacy row from both Supabase and the Sheet via a one-off script (discarded after running, same pattern as earlier test-data cleanups this session) - verified afterward that exactly one "Thor: Ragnarok" row remains with the Rotten Tomatoes link intact. Full `tsc --noEmit` pass clean across every workspace after all four fixes.

2026-09-14 (same day, still later): User asked to (a) actually check the Letterboxd two-thirds gap the previous message had only flagged, and (b) find out why the new Thor: Ragnarok scan's own automated Rotten Tomatoes lookup never fired even though the legacy duplicate had a real RT link. For (b), fetched the real RT page directly (`https://www.rottentomatoes.com/m/thor_ragnarok`, which itself redirects to `.../thor_ragnarok_2017`) and found `lookupRottenTomatoesPage`'s name-match check compares OMDB's bare title against RT's own schema.org `name` field, which is actually `"<Title> (<Year>)"` - "Thor: Ragnarok (2017)", not "Thor: Ragnarok" - so the exact-match comparison silently failed for any film whose RT page follows this common naming convention, not just this one. Fixed by stripping a trailing `" (YYYY)"` before comparing, and separately switched the returned URL from the guessed pre-redirect slug to the fetch's actual final URL (`res.url`), since RT redirects a bare/no-year slug to the real disambiguated one anyway. Verified live via a standalone script against the real case.

For (a), fixed the same unpaginated-query bug in `scripts/src/import-letterboxd.ts` (same pattern as `fieldOptions.ts`/`find-existing/route.ts`), added a permanent `--verbose` flag (prints every row that would actually change plus the full manual-review list, not just counts) and a diagnostic distinguishing "matches found" from "matches that would actually change a value" (225 of 271). Presented both lists to the user in full rather than just a summary, per their explicit choice on an `AskUserQuestion`. The user then resolved essentially every one of the ~18 genuinely-ambiguous multi-copy/format-mismatch items by hand, in one long message, each with real physical-media detail - working through it surfaced several things worth recording:
- **Two more real, unrelated data-entry gaps found along the way, not import-script bugs**: Dawn of the Dead's whole 4-row collection (3 cuts + header) was recorded as plain "Blu-Ray" when the real boxset is 3 4K UHD Blu-Ray discs (one per cut) plus one Blu-Ray special-features disc - fixed the `format` column on all 4 rows. Separately, the user believed a Blu-Ray "Bride of Frankenstein" was missing entirely from the Universal Monsters Essential Collection - it wasn't missing, it existed under `"The Bride of Frankenstein"` (leading "The"), which is why searching the Sheet for "Bride of Frankenstein" didn't surface it; that row was already fully correct on `watched`/`watched_disc`, just needed `disk_region` (was "4", a DVD-style region code left over from some default, corrected to "B") and a TMDb id/link filled in from what the user provided.
- **`normalizeTitleForMatch`'s `CUT_SUFFIX_WORDS` list doesn't recognize "Argento Cut" or a whole-boxset title like "... 4K Boxset"** - "Dawn of the Dead The Argento Cut" and the "Apocalypse Now The Final Cut 4K Boxset" collection header were never matched by the Letterboxd import at all (silently, no review-queue entry either, since they never even became a candidate), fixed by hand rather than expanding the shared cut-suffix regex mid-task (flagged as a minor backlog item instead, since that regex is also used for OMDB-runtime-trust logic elsewhere and deserves its own look rather than a rushed edit).
- **One real previously-wrong attribution got reversed**: American Graffiti's `watched_disc` had ended up on the Francis Ford Coppola Masterpiece Collection copy from an earlier session, but the user clarified (again - "we already went through this one last time round") they watched the standalone disc, not the collection one; swapped `watched_disc` between the two rows.
- **The `letterboxd_review_queue` table/page reference in `import-letterboxd.ts`'s `--apply` path was genuinely dead code** (the table was dropped in 0016 once the original one-time review pass finished) - removed the write attempt entirely in favor of the new `--verbose` listing, rather than leaving it to silently no-op against a nonexistent table.
- One item stayed a disclosed guess rather than a certainty: Pirates of the Caribbean: Dead Man's Chest's "plastic case vs. slip cover" DVD distinction was inferred from which of the two owned DVD rows already had a dedicated special-features disc recorded, not confirmed by the user directly - flagged as such in the report back to them.
- Two items stayed genuinely unresolved (no distinguishing detail existed for either near-identical owned copy, and none was asked for): War of the Worlds (1953)'s two DVD copies, and Land of the Dead's two DVD copies.

Ran the corrected `--apply` first (271 rows updated in Supabase, 490 cells mirrored to the Sheet), then a second one-off script (`apply-letterboxd-corrections.ts`, discarded after running) applying all 19 of the manual disambiguation patches above to both Supabase and the Sheet, verifying the trickiest ones afterward (the American Graffiti reversal, the Bride of Frankenstein TMDb/region fix, Dawn of the Dead's format fix) via direct REST queries. Full `tsc --noEmit` pass clean across every workspace throughout.

2026-09-14 (same day, still later): User gave real eBay Developer credentials in chat - sandbox-only (App ID/Dev ID/Cert ID, all containing `SBX`), with production access approval still pending (~1 day out). Stored them in the gitignored `apps/web/.env.local` and `scripts/.env` (matching this repo's actual env-file layout - there is no repo-root `.env.local`; confirmed both files are covered by existing `.gitignore`/`apps/web/.gitignore` rules and never appear in `git status`), plus documented placeholder entries in both `.env.example` templates. Made `packages/backend/src/estimatedValue/scrapers/ebay.ts` environment-aware: a new `EBAY_ENVIRONMENT` var ("sandbox" | "production", defaulting to production if `EBAY_CLIENT_ID`/`EBAY_CLIENT_SECRET` are set, else sandbox) selects between sandbox (`api.sandbox.ebay.com`, `EBAY_SANDBOX_CLIENT_ID`/`EBAY_SANDBOX_CLIENT_SECRET`) and production (`api.ebay.com`, `EBAY_CLIENT_ID`/`EBAY_CLIENT_SECRET`) OAuth/Browse endpoints, with the token cache keyed by environment so the two can never cross-contaminate; `search()` still returns `[]` gracefully when the selected environment's creds are missing. Added a prominent doc comment: sandbox only returns eBay's fake test-catalog data, never real prices, so it's for verifying the integration mechanically, not for real `estimated_value` data.

Also did a compliance pass on eBay's API terms for this use case (personal, non-commercial, stores price/image/title in a private table, re-checks every ~6 months) before the credentials go live for real. `developer.ebay.com`'s actual docs pages 403'd every fetch attempt (same as an earlier pass), so findings come from search-indexed excerpts, not a direct primary-source read - documented as such. Key points: 5000 calls/day rate limit is a non-issue at this scale; no explicit Browse-API-specific caching-duration ceiling was found (a ~30-day figure that surfaced reads as a post-termination data-destruction obligation, not an active-use TTL, unconfirmed either way) - the existing 6-month refresh-and-replace design is the safe default already in place; the general "eBay Content" clause restricts copying/storing/modifying without an explicit private-cache carve-out, so kept eBay images on the same private-bucket/signed-URL treatment every other retail image in this feature already gets; found public-display and EPN/affiliate attribution rules, neither of which applies to a private, non-affiliate, read-only-search use case. One new flag for later: eBay's own docs frame "Buy APIs" production access as "Limited Release... eBay partners only" via the Partner Network - context suggests this is really about the checkout/commission resources rather than plain `item_summary/search`, but worth a quick sanity check once the user's production approval lands. Full findings and sourcing written into `Claude/TECH STACK AND ARCHITECTURE/estimated-value.md`'s eBay section. Verified with `tsc --noEmit` on `packages/backend` - clean except two pre-existing, unrelated errors (missing `cheerio` install for the concurrent `marbecks.ts` work already reflected in an uncommitted `package.json` diff), confirmed `ebay.ts` itself produced zero errors.

2026-09-14 (same day, a background agent): User asked for four new columns end-to-end, specified in full up front: `personal_rating` (Letterboxd's own star rating, doubled onto a 10-point scale), rental tracking (`is_currently_rented_out`/`rented_by_who`/`date_rented`), and `original_language`. Built `0022_add_rental_and_language_fields.sql` for all of it in one migration (the number moved from the agent's original `0021` once merged into `main`, to avoid colliding with the also-`0021`-numbered concurrent Estimated Value feature - see `INDEX.md`'s BACKLOG).

`personal_rating`: extended `scripts/src/import-letterboxd.ts` (not a new script) to also load `ratings.csv` (Letterboxd's one-row-per-film rating export - previously only `watched.csv`/`diary.csv` were read at all) and a diary entry's own `Rating` column as a fallback, converting via `ratingToPersonalRating` (0.5 -> 1, ... 5.0 -> 10) and writing it to every owned copy of a matched title in both the single-copy and multi-copy branches - exactly parallel to how `watched`/`last_watched_date` already propagate regardless of whether `watched_disc` resolves, since a personal rating is a per-film fact rather than a per-disc one. Deliberately no new manual-review UI for this field, per the user's own "let it happen in the background" framing - it just rides along on the same title-matching confidence the script already has for `watched`.

Rental tracking + `original_language`: added to `ConfirmScreen.tsx` - a plain checkbox ("Currently rented out", same visual as the Watched/Watched Disc checkboxes) reveals `rentedByWho` (`AutocompleteInput`) and `dateRented` (a genuinely new native date picker, `@react-native-community/datetimepicker` - the first date field on this screen backed by a real picker rather than free text) when checked; unticking it clears both fields in the UI and forces them to `null` again in the submitted payload, same "hide + clear, don't just hide" precedent as Special Features Disc Count/Format - this matters specifically so an Overwrite of an already-rented row can actually un-rent it. `original_language` is a plain always-visible `AutocompleteInput` in the Classification group; `fieldOptions.ts` gained `rentedByWho`/`originalLanguage` via the scalar `distinctSorted` pattern. Considered wiring TMDb's own `original_language` field (already fetched for Rating/Studio/isAnimated, so free) as an auto-fill source and decided against it for this pass - TMDb returns a bare ISO 639-1 code, not the human-readable name this field's autocomplete vocabulary expects - logged as a real backlog candidate in `INDEX.md`.

All four fields wired end-to-end: `packages/shared/src/types.ts`, `titleParsing.ts` (`HEADER_ALIASES`/`AUTO_CREATE_COLUMNS`/`parseSheetRowToTitle`/`SHEET_FIELD_FORMATTERS` - real Sheet columns for all four, following the `disc_condition`/`watched` precedent), and `apps/web/src/app/api/scan/confirm/route.ts` (rental fields + `original_language` from `manual.*`; `personal_rating` deliberately untouched there - only ever written by the import script). `sanitize-public-repo.ts` needed zero changes - `import-letterboxd.ts` was already excluded from the earlier Letterboxd work, and none of these four columns carry any privacy concern.

Built `scripts/src/backfill-commando-rental.ts` for the one real specific backfill requested: "Commando" (the user's real Arnold Schwarzenegger collection copy) is out on loan by accident. Dry-run by default like `import-letterboxd.ts`, queries by title + `name_of_collection ilike '%schwarzenegger%'`. **Not yet run against real Supabase** - needs the user to run it by hand once credentials are available, and the exact day/year for the loan (only "February" was given) still needs confirming - the script defaults `date_rented` to `2026-02-01` as an explicit placeholder.

This work happened in its own git worktree, merged into `main` afterward alongside the concurrent Estimated Value feature's own uncommitted work and a separate peer interactive session's own uncommitted Letterboxd/Rotten Tomatoes fixes (see above) - all three coexisted in the same working tree without any actual conflict once `packages/shared/src/types.ts` was manually reconciled (both features' new fields kept, placed in their own sections) and `import-letterboxd.ts` was three-way-merged by hand (git's own `merge-file`/`git apply` tooling choked on this repo's CRLF line endings, so the peer session's Thor Ragnarok/Rotten Tomatoes fixes and this agent's `personal_rating` additions were reconciled by direct inspection of both diffs instead). Full `tsc --noEmit` clean across `packages/shared`, `packages/backend`, `apps/mobile`, and `scripts` in the worktree before merging.

2026-09-15: Four separate threads in one session, with a second interactive Claude Code window active on the same working directory concurrently (its own Estimated Value Sheet-sync edits kept landing in `apps/web/src/app/api/scan/confirm/route.ts` mid-session, reconciled cleanly since they touched different parts of the file). (1) User asked for a subagent to investigate how to keep two concurrent Claude Code windows on this repo from interfering with each other; findings (Claude Code's own `EnterWorktree` mechanism is already used here but got left unmerged once - `worktree-agent-a2e63f54be3c45443` - plus real collision risk on Sheet migration numbering and freeform `Prompt Journal.md`/`To Do list.md` edits) were written into `repo-layout.md` as a standing rule: a worktree branch must be merged back or abandoned the same day it's created, never left open across sessions. (2) A second subagent ran `apps/mobile`'s Expo dev server once to confirm it boots cleanly, then stopped it - the user then found the mobile app "wouldn't launch," which was simply that stopped dev server; restarted it for real. Separately, a live "queue scan failed" report turned out to be `apps/web`'s own Next dev server never having been started at all (the mobile scanner's entire backend - `/api/scan/*` - lives there, per `EXPO_PUBLIC_SCAN_API_URL`) - started it, and recorded as a standing note that "run the app" for this project means both dev servers together, not just Expo. (3) Investigated why `personal_rating` (see 2026-09-14's entries above) had no real data despite `ratings.csv` genuinely having 743 rated films: confirmed via a live dry run of `import-letterboxd.ts` (no `--apply`, so no writes) that 270 of 271 matched rows would still change - the `--apply` re-run mentioned in the 2026-09-14 INDEX.md backlog note happened *before* the `personal_rating` column/logic existed at all, so it was never actually applied for real once that logic was added later the same day. Flagged to the user rather than run `--apply` unilaterally, per this project's own standing rule to ask before writing to the real collection. (4) Built the TMDb auto-fill for `original_language` end-to-end, following the exact Rating/Studio precedent: `packages/backend/src/iso639.ts` (new, a static ISO 639-1 -> English name table - this is what the 2026-09-14 backlog note said was the missing piece), `original_language_is_manual` (`0023_add_original_language_is_manual.sql`, defaulting `true` so every existing row stays protected from `refreshTmdbFields`), and `showOriginalLanguageField` in `ConfirmScreen.tsx` moving the field out of the always-visible Classification group into the same TMDb-hides-it-when-resolved block as Rating/Studio (including the multi-title-collection exclusion, so a box set's cover text can't leak into every member's language). Full `tsc --noEmit` clean across `packages/shared`, `packages/backend`, `apps/mobile`, and `apps/web` afterward. Docs updated in place: `barcode-review-screen-fields.md`'s Original Language note and `INDEX.md`'s backlog entry both now describe the shipped design, with the original manual-only reasoning kept for context.

2026-09-15: User asked to re-check the finished Estimated Value feature (`estimated-value.md`) against the original spec for anything missing. Read every relevant source file directly rather than trusting the doc alone, and found two real gaps the doc hadn't flagged:
1. The spec's "if the result is on sale, grab the original price" was never implemented anywhere - every scraper only ever read the current (possibly discounted) listing price.
2. The Google Sheet - one of the two places the spec explicitly asked for this column ("add a new column to the database/spreadsheet") - had zero wiring for it: no `HEADER_ALIASES` entry, so a computed value never reached the Sheet and a manually-typed one would have been silently ignored by the webhook.
Asked the user two clarifying questions rather than guessing: for on-sale handling, confirmed "store the original (pre-sale) price only," not both prices side by side; for the Sheet, confirmed full two-way sync (a manual Sheet edit updates Supabase, and the computed value pushes back out) over a one-way-only push, matching how every other column already behaves.

Implemented both, live-verifying instead of guessing wherever possible: fetched JB Hi-Fi's real `suggest.json` response live and confirmed the exact field name (`compare_at_price_max`, `"0.00"` when not on sale) rather than assuming one - wired into `shopifySearch.ts` (used when it's a real number greater than the current price). Added the same treatment to `ebay.ts` via the Browse API's documented `marketingPrice.originalPrice` (flagged as unverified live, same as the existing API-compliance section, since developer.ebay.com 403s fetch tools). Live-checked Marbecks' real search-results markup too and found no sale-price signal exists there at all - documented as a real finding, not a silent no-op.

For the Sheet: added a synthetic `estimated_value_display` column mapping (`"Estimated Value"` header) to `titleParsing.ts`'s `HEADER_ALIASES`, with a small `parseEstimatedValueCell`/`formatEstimatedValueCell` pair handling the `"$13.50"` / `"est. $45.00"` / `"n/a"` round-trip - deliberately not importing the equivalent formatter already sitting in `fallbackEstimate.ts`, since that module is excluded from the public repo outright and `titleParsing.ts` isn't (an import across that boundary would dangle once the public copy's sentinel-marked block gets stripped). New `apps/web/src/lib/estimatedValueSheetSync.ts` (`pushEstimatedValuesToSheet`) pushes freshly-computed values back out, wired into all three places a price actually changes: the scan-time trigger, both paths of the 6-month recheck sweep, and a resolved manual review. `RefreshBatchResult` grew a `titleIds` field so callers know what to push without re-deriving it. Updated `sanitize-public-repo.ts` (`titleParsing.ts` added to the sentinel-marked shared files, the new sheet-sync file excluded outright) and `estimated-value.md` (new "On-Sale Original Price" and "Google Sheet Sync" sections, plus a fix for a stale GET-vs-POST doc/code mismatch noticed along the way). `tsc --noEmit` clean on `packages/shared`, `packages/backend`, and `apps/web` (one pre-existing, unrelated `TmdbFields.originalLanguage` error already sat in the working tree before this session started - confirmed via `git stash` - and is untouched by this work).

2026-09-15 (same day, follow-up): User tested the semi-automatic system against 5 real titles and pushed back hard: only 1 of 5 (Captain Marvel) got a real price, the other 4 fell through to the random fallback silently, and asked for Pending Value to become the primary workflow instead - send everything found to the user for manual accept/reject rather than auto-accepting or silently dropping low-confidence results. Also flagged that Amazon "should be working" since they can see Marvel movie listings on it directly, and asked to reset the 5 test titles' data and find the missing Sheet column.

Investigated rather than assumed on both fronts before touching code: (1) re-verified live (via `WebFetch` and a `curl` with a real browser user-agent) that both amazon.com and amazon.com.au still return a flat 503 to any automated request - confirming Amazon was never actually wired in as a live source at all (dropped back on 2026-09-14 per this doc's own Retailers table) and that the user's manual browser check and this codebase's scraper access are simply unrelated; nothing had regressed. (2) Ran a dry-run query against the live private Supabase project confirming exactly the 5 named titles and their current values (4 of 5 genuinely were `estimated_value_is_estimate = true`, i.e. fallback guesses, not real prices - Captain Marvel alone was real), then reset all 5: cleared `estimated_value`/`estimated_value_is_estimate`/`estimated_value_last_checked` to null/false/null, deleted their `title_price_observations` rows (1) and `pending_value_review` rows (2), via a temporary script written to `scripts/src/`, run, and deleted immediately after (never committed) - same "never leave test/cleanup scripts behind" precedent as the 2026-08-28 backfill-match verification. (3) Read the live Sheet header directly (also via a temporary script, same cleanup) and confirmed no "Estimated Value" column existed - this was a real design gap from the same-day Sheet-sync work: `AUTO_CREATE_COLUMNS` was deliberately not extended for it at the time (reasoning: "the value only means something once the estimator has actually run"), which in hindsight just left the user with no way to know it needed adding by hand. Added it directly via the Sheets API instead of asking the user to do it themselves - it landed at column AS, expanding the grid from 44 to 45 columns first (same `gridProperties.columnCount` check-and-expand pattern `sync-sheet.ts` already uses).

Asked 4 clarifying questions before redesigning the core pipeline, all answered with the recommended option: remove the 70% auto-accept tier entirely (every title with candidates goes to Pending Value, no exceptions, not even near-certain matches); no confidence floor either - keep lowering until something shows up, with the random fallback reserved only for the case where literally nothing survives the hard format/year gate at all; raise the review candidate cap from 7 to 15 rather than showing everything unbounded; and leave the NZ-first/international-only-if-NZ-empty search ordering unchanged. Implemented: `confidence.ts` lost `bandForConfidence`/`ConfidenceBand`/the 70%/30% thresholds entirely (confidence is now pure ranking, never a gate) - documented in place as a `PENDING-VALUE-ALWAYS-MANUAL` comment block explaining the redesign and its rationale for future sessions. `refresh.ts`'s `processTitle` dropped its whole auto-accept branch (`recordAcceptedPrices` is no longer called from here at all, only from the Pending Value response route once a human decides); every candidate surviving the hard gate now goes straight to `pending_value_review`, sorted best-first and capped at a new `MAX_REVIEW_CANDIDATES = 15`; the fallback estimate path now only fires when the scored list is empty. `RefreshBatchResult` lost its `autoAccepted` field (updated the two log lines in `instrumentation.ts` and `refresh-estimated-value.ts` that referenced it). No mobile changes were needed at all - `PendingValueDetailScreen.tsx` was already generic over however many candidates the API sends, with no hardcoded "7" anywhere. Rewrote `estimated-value.md`'s Confidence Scoring/Manual Review/Averaging/Fallback sections in place to describe the new always-manual design, with the old auto-accept/threshold design kept visible via strikethrough-style "redesigned 2026-09-15" framing rather than deleted outright, per this project's own backlog convention. `tsc --noEmit` clean across `packages/shared`, `packages/backend`, `apps/web`, `apps/mobile`, and `scripts` afterward. The 5 reset titles are now ready for the user to trigger a fresh scan-time or manual refresh run and see the new all-manual Pending Value flow for real.

2026-09-15 (same day, second follow-up): Live results from the redesign above surfaced real noise - non-disc junk (a Sonos speaker, a phone case, a CD box set, a t-shirt) sitting alongside genuine movie candidates now that the confidence floor no longer hides anything. Flagged it and the user asked to apply Marbecks' existing non-disc filter to "all other vendors." Investigated live before copying it verbatim, since Marbecks' filter works by requiring an explicit DVD/Blu-ray/4K/UHD keyword to appear in the listing text - fetched JB Hi-Fi's real `suggest.json` response for "Thor Ragnarok" and found its genuine, correct "Thor: Ragnarok" listing has `product_type: "MOVIES"` with no format keyword anywhere in its title/type/body; copying Marbecks' allow-list approach as-is would have silently dropped every real JB Hi-Fi match, the opposite of what was wanted. Fetched Real Groovy's real response too and found its `product_type` is the literal opposite - genuinely `"DVD"`/`"LP"`/`"CD"` for real listings. Designed `shopifySearch.ts`'s `isLikelyDiscProduct` around that difference: a `type` that's an explicit disc-format word is kept, a `type` that's a known non-media department (music/cd/lp/games/accessories/apparel/books/etc.) is dropped, anything else (an unrecognized generic name like "MOVIES") is kept by default - false negatives (hiding a real match) treated as worse than false positives (one extra junk candidate rejected by hand). Added the equivalent `isLikelyDiscListing` to `ebay.ts` using a text-based exclude check instead (no clean category field to read there, and developer.ebay.com's docs still 403 fetch tools), targeting common movie-search cross-listing junk (funko pops, posters, soundtrack vinyl, apparel, video games). Re-queued and re-ran the same 5 test titles to verify live: Casper's Haunted Christmas dropped from 12 to 10 candidates (the speaker and phone case gone) and Paper Planes from 10 to 7 (the CD set, a book, and a t-shirt gone); the other 3 titles' counts were unchanged since their noise was genuine-but-wrong-movie discs, not non-disc merch - confirmed this is a real, expected limit of the filter (search relevance, not media type, which is what confidence ranking + manual reject are for) rather than a bug, and documented it as such.

Mid-turn, user also asked for a "Reject All" button on Pending Value, prompted directly by seeing Avengers: Infinity War's 15-candidate, all-wrong-movie case from the same testing. Added `rejectAll` to `PendingValueDetailScreen.tsx` - bulk-fills every candidate's decision to reject, Submit still required afterward, no auto-submit. `tsc --noEmit` clean across `packages/backend` and `apps/mobile` afterward. `estimated-value.md` updated with a new "Non-Disc Result Filtering" section and a note on the Reject All button.

2026-09-15 (same day, third follow-up): User asked to simplify the review screen further - rather than an explicit Accept/Reject choice on every candidate, check the ones you want and leave the rest, with unchecked candidates counting as rejected; suggested dropping the Reject button for a plain checkbox instead. Checked `pending/respond/route.ts` first and confirmed it already only reads `action: "accept"` entries out of the submitted decisions list, treating anything absent as not-accepted - so this needed zero backend changes, purely a `PendingValueDetailScreen.tsx` rewrite. Replaced the `decisions: Record<index, "accept"|"reject">` state with a plain `accepted: Record<index, boolean>`, removed `setDecision`/`allDecided`/the per-candidate Accept+Reject button pair, and removed the same-day "Reject All" button too since it's now redundant - leaving every box unchecked already means "reject all." Each candidate card now has a single checkbox + "Accept" label; Submit is always available (no more "decide on everything first" gate) and sends only the checked indices. `tsc --noEmit` clean on `apps/mobile`. `estimated-value.md`'s Manual Review section rewritten in place to describe this as the third and current iteration of the decision UI, with the two earlier same-day designs (explicit accept/reject pair, then +Reject All) kept visible for context rather than erased.

2026-09-15 (same day, fourth follow-up): User asked to bring "Reject All" back "for clarity" and have Submit refuse to fire with fewer than one candidate selected - a direct correction of the third iteration's removal reasoning above (that pass assumed an explicit bulk-reject button was now pointless once unchecking was already the default state, but the user wants it kept as a distinct, explicit action rather than relying on an implicit "just don't touch anything"). Restored the button, but gave it a different job than before: rather than a pure state-setting no-op that fills every checkbox to a "reject" value that no longer exists in this model, `rejectAll()` now resolves the review immediately with an empty accepted list (`respond([])`) - the exact API call an all-unchecked Submit would have made. This is also why Submit itself needed the new gate (`submitDisabled = submitting || !anyAccepted`): with Reject All now owning the "nothing here is right" path outright, an empty-list Submit tap would just be an ambiguous, accidental duplicate of it. Split the old inline `submit` body into a shared `respond(payload)` helper both `submit()` and `rejectAll()` call, so the actual API request/error-handling/loading-state logic lives in exactly one place. `tsc --noEmit` clean on `apps/mobile`. `estimated-value.md`'s Manual Review section now describes all 4 same-day iterations of this screen's decision UI as one continuous narrative, since a future session reading only the final state would otherwise have no way to know Reject All was ever removed and re-added, or why Submit's disabled condition exists at all.

2026-09-15 (same day, fifth follow-up): User closed the loop with one more gate: disable Reject All whenever a candidate is checked, so it can't silently discard an in-progress accept and resolve to the random estimate instead. Added `rejectAllDisabled = submitting || anyAccepted` alongside the existing `submitDisabled = submitting || !anyAccepted` - the two are now mutually exclusive by construction, exactly one of Submit/Reject All enabled at any time based purely on whether anything is checked. `tsc --noEmit` clean on `apps/mobile`. This is the 5th and (for now) final iteration of this screen's decision UI in one session - saved as a durable lesson in memory (`dont-remove-ui-actions-for-logical-redundancy.md`) after the 3rd->4th correction, rather than repeated here.

2026-09-15 (same day, sixth follow-up): User reported the confidence score itself has proven accurate in real use and asked for a strict 0%-confidence floor (candidates scoring exactly 0 never shown), pushed for real international search coverage given how thin NZ retail options are, asked whether Amazon's data was reachable through anything other than scraping, and asked why eBay hadn't produced a single candidate despite definitely having secondhand Marvel-movie listings in stock.

Investigated eBay before touching code rather than assuming: found two compounding reasons it never showed anything. (1) `gatherCandidates`'s "NZ first, international only if NZ returns nothing" rule (confirmed with the user only in the redesign a few messages ago) meant eBay was never even called across any of the 5 test titles, since JB Hi-Fi/Real Groovy always returned *some* raw candidates - even entirely wrong-movie ones - which counted as "NZ found something." (2) Checked `apps/web/.env.local`/`scripts/.env` directly (lengths/emptiness only, never printing secret values) and found `EBAY_ENVIRONMENT` is still `sandbox` with empty production credentials, meaning even a triggered eBay call could only ever have returned eBay's fake test catalog, never a real listing - flagged to the user as something to check on their eBay developer dashboard, since production access was "~1 day out" as of 2026-09-14.

Researched Amazon access live rather than repeating the 2026-09-14 finding from memory: `webservices.amazon.com/paapi5`'s own docs page now redirects straight to a deprecation notice - PA-API 5 calls get a flat 403, being replaced by a new "Creators API" - and that replacement's own landing page requires signing into an existing Amazon Associates account before showing anything at all, i.e. still gated behind the same sales-generating-affiliate-account requirement that made PA-API a dead end for a personal project in the first place. Checked Keepa (a third-party Amazon price-tracking service) as a non-official alternative but couldn't verify its current API terms live (keepa.com blocks fetch tools) - flagged as an unconfirmed lead for the user to check directly rather than asserted as workable. Net finding: no viable Amazon path exists, official or third-party-confirmed.

Implemented the two code changes directly requested: `confidence.ts`-scored candidates now filter to `confidence > 0` before capping at `MAX_REVIEW_CANDIDATES` in `refresh.ts` (a floor at zero, not a return of the old 30% band - anything above 0% still shows), and the fallback-estimate trigger widened to match (nothing survived the hard gate, or everything that did scored a flat zero). `gatherCandidates` now always queries every NZ source AND every international source every time, removing the early-exit that caused eBay's finding (1) above - a direct reversal of the "keep NZ-first, international only if empty" answer from a few messages ago, superseded by real test evidence. `tsc --noEmit` clean on `packages/backend`.

Also live-probed a few Shopify-powered specialty import/cult-movie retailers (Vinegar Syndrome, DiabolikDVD - both responded 200 on the same `/search/suggest.json` endpoint JB Hi-Fi/Real Groovy already use; Zavvi 404'd, HMV 403'd) as quick-to-integrate candidates given `shopifySearch.ts` is already a shared, reusable implementation - not yet added, flagged to the user as an option pending their steer on which international sources are actually worth pursuing next, per this project's own standing rule to ask before taking on an open-ended expansion unilaterally.

Asked two follow-up questions and the user said yes to both: add Vinegar Syndrome/DiabolikDVD now, and keep researching more mainstream international retailers. Live-probed ~12 more candidates (checking actual response content, not just HTTP status, after DiabolikDVD's own 200-but-not-really-Shopify result taught that lesson) - JB Hi-Fi Australia and World of Books both came back genuinely real (JB Hi-Fi AU: a real "Thor: Ragnarok" listing at $24.98 AUD, same "MOVIES" type taxonomy as the NZ store; World of Books: literal `"DVD"`/`"Book"` type values, a UK secondhand book/media reseller). DiabolikDVD and ImportCDs turned out to be false positives (WordPress/ASP.NET pages, not real Shopify APIs, despite both returning HTTP 200) - dropped rather than built. CD Universe is genuinely Shopify but a "thor" search's top result was an unrelated adult product - dropped as unsuitable. Sanity.com.au/MusicMagpie/Base.com/MovieStop/SecondSale/DVD Empire/Deep Discount all failed outright (404/403/301/connection failure).

Built 3 new scraper files (`jbHifiAu.ts`, `worldOfBooks.ts`, `vinegarSyndrome.ts`) and wired them into `refresh.ts`'s `INTERNATIONAL_SOURCES`. This surfaced two real fixes needed in the shared `shopifySearch.ts`: (1) it had hardcoded `currency: "NZD"` since only NZ stores used it before today - changed to a required parameter, each new store passing its own real currency (AUD/USD/GBP), still converted to NZD via the existing `convertToNzd` path; (2) World of Books' out-of-stock listings come back in `suggest.json` at a literal `"0.00"` price, which isn't a real free item and would corrupt the average - added a `priceRaw <= 0` guard alongside the existing `NaN` check. Also caught and fixed a near-miss in `worldOfBooks.ts` itself before shipping it: the first draft copied jbHifi.ts/realGroovy.ts's own "filter obviously-wrong-format text out of listingText" pattern using a "book" keyword, which would have wrongly rejected any real movie whose own title contains the word "book" (The Jungle Book, Guest Book, ...) - removed that specific check and left the safer, structural `isLikelyDiscProduct(type)` filter in `shopifySearch.ts` to do that job instead, since it reads the real `product_type` field rather than guessing from title text. `tsc --noEmit` clean on `packages/backend`. `estimated-value.md`'s Retailers table rewritten with every source tried today (working and dropped), plus new sections documenting the currency-parameter and zero-price fixes. Mid-turn, also reset and re-queued the same 5 test titles (deleting their `title_price_observations`/`pending_value_review` rows and nulling `estimated_value` again) so the user could see the new sources/fixes live in Pending Value - ran in the background this time given 7 sources per title (up from 4) meant it could plausibly exceed the foreground command timeout.

User then asked about adding Shout! Factory (a US cult/genre-catalog home video label) as a source. Live-checked the same way as every other candidate today rather than assuming: both its Shopify-style search endpoint and its plain homepage return a flat 403 with a Cloudflare JS challenge embedded in the response - the identical bot-block that got BuyDVDs.co.nz dropped back on 2026-09-14. Not pursuable without full headless-browser automation, which this feature has avoided everywhere else - dropped, documented in `estimated-value.md`'s Retailers table rather than built.

2026-09-15 (same day, seventh follow-up): User pointed out a real gap - a title with zero candidates found gets a random fallback value written straight to Supabase with no way to see or correct it anywhere in the app, and asked for a Pending Value note covering exactly that case: shows the process ran, tap it to see the title/value/an option to change it.

Checked the codebase's own established pattern before designing anything new: `rating_is_manual`/`studio_is_manual` (0005_add_tmdb_fields.sql) and `original_language_is_manual` (0023, from earlier today) all follow the same shape - a boolean that protects a human-confirmed value from being silently overwritten by later automation. Applied the identical pattern here rather than inventing something new: `0024_add_estimated_value_is_manual.sql` adds `estimated_value_is_manual`, defaulting **false** (the opposite of `original_language_is_manual`'s `true` default) - deliberately, since every existing `estimated_value` came from this feature's own automation, never a human decision, so the migration should surface the whole existing backlog of guesses into the new queue immediately rather than treating them as already-reviewed. `refresh.ts`'s `fetchOverdueTitles` now excludes manually-confirmed titles from the 6-month recheck sweep.

The new "auto-estimated" queue is defined purely as `estimated_value_is_estimate = true AND estimated_value_is_manual = false` - this naturally covers both ways a title ends up needing a look (zero candidates ever found, or the user rejected every real candidate shown) without any path-specific code, since both origins already just set the same two columns. Built two new routes (`auto-estimated/route.ts` list, `auto-estimated/confirm/route.ts` - always marks manual and clears is_estimate whether or not the number changed, then pushes to the Sheet) and a new, deliberately much simpler mobile screen (`AutoEstimatedDetailScreen.tsx` - title, poster, one pre-filled editable value, Confirm) alongside merging both queues into one flat list in `PendingValueScreen.tsx` (a `"review" | "estimated"` row union) rather than a separate screen, since both are "things sitting in Pending Value" from the user's own perspective. `tsc --noEmit` clean across `packages/shared`, `packages/backend`, `apps/web`, and `apps/mobile`.

Discovered mid-build that this session has no persisted Supabase management/personal-access-token env var - checked `scripts/.env`/`apps/web/.env.local` directly and found none, confirming `deployment-and-security.md`'s own documented convention (a fresh short-lived token requested in-chat each time a migration needs applying, never persisted) rather than assuming one was available. Asked the user for a fresh token to actually run `0024` against the live private project, since the new code (`fetchOverdueTitles`, both new routes) will error at runtime referencing `estimated_value_is_manual` until that column exists for real.

2026-09-15 (same day, later, a second interactive window - see the entry above this one for the first): Followed up on the earlier `personal_rating`/Original Language session with real writes and one design ask. Applied `import-letterboxd.ts --apply` for real once the user approved it via an `AskUserQuestion` (the dry run showing 270/271 rows still pending was itself surfaced as a question, not run unilaterally, per this project's standing rule about writing to the real collection): 271/271 rows updated in Supabase, 755 cells mirrored to the Sheet. Separately fixed the actual "queue scan failed" report the user made mid-session: `apps/web`'s dev server had nothing free on port 3000 (an unrelated `Youtube Short Splitter` render script already held it, not a DVD-app process), so it silently came up on 3001 instead while the phone's `EXPO_PUBLIC_SCAN_API_URL` still pointed at 3000; asked the user how to resolve it (repointing the phone's `.env` vs. killing the other render) rather than picking for them, then repointed `apps/mobile/.env` to 3001 and restarted Metro (which needed its own stale process force-killed first - the phone had an `ESTABLISHED` connection to it, confirming it really was still running despite an earlier `TaskStop` on the wrapper task not fully killing the child Metro process on Windows).

2026-09-16: User asked for three things in one message. (1) A planning pass for web-app rental integration, prompted by now having real rental columns in the schema: added a "Rental status control" note to `WEB APP DESIGN.md`'s DVD Pages section (log-as-rented / mark-as-returned, scoped to one disc, same hide-and-clear precedent as everywhere else this pattern appears) and a new "RENTAL DASHBOARD" section (a single list of every currently-rented disc with the same mark-as-returned action, scope deliberately left open beyond that per the user's own "not sure what else goes on it yet") - also logged in `To Do list.md`'s BACKLOG since the actual web browse/DVD-page UI doesn't exist yet to build this against. (2) Backfilled a real rental the user just remembered: "Battle of Britain" (the standalone DVD, not either of the two rows sharing that title inside the "10 Wartime Documentaries" collection - checked all three by exact title first) rented to "Pat" on 2026-09-13 - `scripts/src/backfill-battle-of-britain-rental.ts`, same dry-run/`--apply`/Sheet-mirror shape as `backfill-commando-rental.ts`. (3) Backfilled `original_language = "English"` on five specific titles (Paper Planes, Casper's Haunted Christmas, Avengers: Infinity War, Thor: Ragnarok, Captain Marvel) via a new reusable `scripts/src/backfill-original-language.ts` - all five resolved to exactly one row each by exact title match. Also tried to set `original_language_is_manual` on those five at the same time, since migration `0023_add_original_language_is_manual.sql` (written the previous session) still hasn't actually been applied to either live Supabase project - the script detects the missing column, warns, and falls back to writing `original_language` alone rather than failing the whole backfill; worth a re-run once `0023` is applied for real to fill in the flag too.

2026-09-16 (same day, later): User provided a fresh Supabase personal access token in-chat (used only inline in Management API calls, never persisted to a file) and asked to apply `0023_add_original_language_is_manual.sql` for real - applied to both live projects, verified the column exists on both, and confirmed the five titles from the previous entry already got `original_language_is_manual = true` automatically from the column's `default true` (no extra write needed - they existed before the column was added). Then the user separately noticed the private project is at 300/500MB on Supabase's free plan and asked what happens at the cap and whether the fix is two databases or Pro. Investigated live rather than answering generically: queried actual per-table sizes and found `tmdb_title_index`/`tmdb_tv_title_index` (the fuzzy-typo-correction fallback tables) were 304MB of the private project's 318MB total, while the real collection (`titles`) was only 3.4MB - the "300MB problem" was really "we mirrored TMDb's entire 1.24M-title global catalog for a rare fallback feature," not genuine collection growth. Verified Supabase's actual free-plan behavior at the cap live (read-only mode, not deletion, with an 80%-usage email warning beforehand) rather than assuming. The user then asked about better architectures for the fuzzy-index itself (a different platform, or hitting the API live instead) - explained why "just call the API" doesn't apply here (neither OMDB's nor TMDb's own search is meaningfully typo-tolerant, confirmed live earlier in the project - that's the entire reason a local index exists) and laid out the real alternatives (a dedicated search service vs. a bundled local file) without picking one for them. User picked "dedicated search service"; live-checked actual 2026 pricing before building anything (Typesense Cloud/Meilisearch Cloud both cost ~$20-30/mo at this row count, no meaningful ongoing free tier) and surfaced that this costs about the same as Supabase Pro itself before proceeding, via an `AskUserQuestion` rather than assuming the user still wanted to pay once the real numbers were in. User then asked specifically about free options - checked Algolia's actual pricing page directly (secondary sources disagreed with each other on the current record limit) and found its free plan is genuinely free forever (50K records/10K searches per month, no card), but would still need the catalog trimmed to fit under 50K - at which point the user reasoned correctly that trimming was needed either way, so there's no benefit to a new external service over just trimming Supabase's own table.

User then proposed a smarter dynamic cache (load a genre's titles into a hot set while scanning that shelf, evict afterward) instead of a flat trim - pushed back on this rather than building it uncritically, for two structural reasons found by actually checking: TMDb's bulk export has no genre field at all (would need a per-title detail call for 1.24M rows, the same rate-limited endpoint that's already a bottleneck elsewhere), and the fallback only ever fires *after* a manually-typed title has already failed every other match - so its genre isn't knowable at the moment a cache would need to have preloaded it. User agreed to do the flat trim now and revisit the smarter version later. Executed: `delete ... where popularity < 1` on both tables plus `vacuum full` to actually reclaim disk (a plain delete doesn't shrink the on-disk file on its own) - movies 1.24M rows/246MB -> 380K rows/70MB, TV 231K rows/58MB -> 125K rows/31MB, private project total 318MB -> 116MB. Added the same `MIN_POPULARITY = 1` filter to both `refresh-tmdb-title-index.ts`/`refresh-tmdb-tv-title-index.ts` so a future re-run doesn't quietly re-import the full catalog and undo this. Full writeup (including the three rejected alternatives and their real numbers, not just the one that got built) in `database-design.md`'s new "TMDb Title Index storage trim" note.

2026-09-15 (continuing the RapidAPI Amazon thread from earlier the same day, across a context reset): Previous session asked about RapidAPI's risk profile, then the user pushed back with a specific, well-reasoned case for still pursuing Amazon: NZ/AU physical media retail is shrinking as studios pull investment for streaming, and Amazon's catalog covers obscure titles no local site does. Agreed to a free-tier-only trial (never wired into the automatic pipeline - the free caps couldn't sustain it) and live-verified a real provider ("Real-Time Amazon Data" by OpenWeb Ninja, 45k subscribers, confirmed via an actual browser since RapidAPI's marketplace pages are JS-rendered and plain `WebFetch` shows nothing) before building `rapidApiAmazon.ts`. Mid-build, the user proposed running several unofficial Amazon APIs at once, each burning its own separate free quota, rather than being stuck at one provider's 100/month cap - live-verified this is genuinely possible (RapidAPI tracks quota per-API-subscription under one account/key, not per-account) and went looking for a second real candidate right as the session hit its context limit and had to stop mid-check.

Also mid-turn that session, the user pasted real production eBay credentials directly into chat (asking for "an agent" explicitly, so dispatched one rather than doing it inline) - the agent wired them into both `.env` files correctly and left behind three debug scripts mid-verification when the session was interrupted before it could report back or clean up.

This session picked up after a context reset. Checked the interrupted eBay agent's actual state first rather than assuming either success or total loss: found the credentials were written correctly (matched what the user pasted) and three leftover `__temp-*.ts` scripts showed exactly where verification had stopped. Ran the leftover OAuth debug script directly - eBay's real production token endpoint returns `401 invalid_client`. Compared the pasted Cert ID's shape against eBay's normal Cert ID format and found a concrete discrepancy (28 hex characters after `PRD-` vs. the standard 32-character UUID shape), flagged to the user as a likely copy-paste truncation rather than guessing at a fix. Cleaned up all three leftover scripts. Confirmed migration `0024` (from the same interrupted-session boundary) still isn't applied to the live database either - flagged, not blocking.

Resumed the Amazon-provider search exactly where it left off: found "Axesso - Amazon Data Service" (a "Verified API," 10+ years in business per their own listing, 1287 RapidAPI subscribers) also offers a genuinely free, 100/month hard-capped BASIC plan - confirmed its real response schema live via its own playground, which turned out meaningfully different from the first provider's (bare numbers instead of dollar-strings, a `retailPrice` field for the original/RRP price, a relative product URL needing the domain prepended, no explicit currency field at all). Built `axessoAmazon.ts` to match. Also checked a third candidate ("Realtime Amazon Data" by Data Extractor Pro) and deliberately rejected it: only 25 free requests/month, and NOT hard-capped - it bills $0.10/request past that, real uncapped financial exposure from a stray extra run, from a slower and less-established "unofficial" provider - not worth it next to two providers offering a real free hard cap. Updated `trial-amazon-rapidapi.ts` to query both integrated providers per search title side by side. Wired both scrapers into `packages/backend/src/index.ts`'s exports, registered `trial-amazon-rapidapi` as an npm script in both `scripts/package.json` and the root `package.json` (with matching `stripNpmScripts` entries in `sanitize-public-repo.ts` so the public build doesn't ship dangling references to either), added the file to `EXCLUDE_FILENAMES`, and documented `RAPIDAPI_KEY` in `scripts/.env.example` (one key works for both providers under a single RapidAPI account, but each needs its own free subscription activated). `tsc --noEmit` clean on `packages/backend` and `scripts`. `estimated-value.md` rewritten with a full "AMAZON VIA RAPIDAPI" section covering both providers, the rejected third one, and why none of this touches the automatic pipeline.

2026-09-15 (same day, later): User provided a fresh Supabase personal access token in-chat and asked to apply `0024_add_estimated_value_is_manual.sql` for real. Applied via the Management API's `database/query` endpoint against the private project only (`lpbsypfwhjluwuosrgnd` - this migration is private-only, same as `0021`), token used only inline in that one curl call, never written to a file. Verified via a throwaway script (deleted immediately after) that the column exists on `titles` and defaults to `false` as designed. The auto-estimated-note feature (built earlier this session, blocked on this migration) is now fully live end-to-end.

2026-09-17: User asked for a status report on the whole Estimated Value feature (given verbally, not written to a doc - the existing `estimated-value.md` already serves as the detailed record). Then four follow-ups in one message: (1) asked how to find the *correct* eBay Cert ID step by step, given last session's finding that the pasted one looked short - answered as a walkthrough (developer.ebay.com's Application Keys page, Production tab, using the page's own "click to reveal/copy" control rather than manually dragging to select across dash-separated segments, which is the likely original cause of the truncation). (2) Asked for more third-party Amazon APIs to further multiply the effectively-free request budget beyond the two already integrated. Searched RapidAPI's real search (typing into its search box - direct `/search/<term>` URL navigation doesn't actually filter results, confirmed again) for "amazon" and "amazon scraper": found one genuine new candidate, "Price Analytics" by 3B Data/@CRAWLER2API - covers Amazon *and* Google Shopping/eBay/Idealo/billiger in one API, BASIC plan $0/mo with a 100/month hard limit (same shape as the two already built). Flagged rather than built outright: it uses a session-job-and-poll model (submit a query, get a job ID, poll for the result) rather than the simple synchronous GET-and-parse pattern every other scraper in this feature uses, and its listing hasn't been updated in 2 years (vs. 2 months/6 days for the two already integrated) - a meaningfully bigger integration lift and a real staleness flag, surfaced to the user rather than built silently. (3) Verified the 5 recurring test titles (Paper Planes, Casper's Haunted Christmas, Avengers: Infinity War, Captain Marvel, Thor: Ragnarok) are still sitting in Pending Value with real candidates (2/9/7/5/10 respectively) - lower counts than the last reported batch (7-15 each), consistent with the 0%-confidence floor now actually being in effect, though the exact run that produced these wasn't directly witnessed this session (plausibly from state already in place before the context reset). (4) Started both dev servers in the background per the standing "running the app means web and mobile together" rule - `next dev -p 3001` (matching `apps/mobile/.env`'s existing `EXPO_PUBLIC_SCAN_API_URL`) and `expo start`. Caught a real potential issue before declaring it done: Next.js's own startup log advertised a *different* LAN IP (192.168.227.1, apparently a secondary/virtual adapter) than the one the phone is actually configured for (192.168.1.71) - rather than assuming that was fine, confirmed live with a direct request to the phone's actual configured address and got a real `401` (correct rejection of a bad secret, not a connection failure), proving the server is genuinely reachable there despite what its own log happened to print.

2026-09-17 (same day, follow-up): The eBay Cert ID walkthrough from the entry above turned out to be chasing the wrong cause entirely - the user sent a screenshot of the actual eBay Developer dashboard showing the Production keyset itself marked "currently disabled," with a compliance message about the Marketplace Account Deletion/Closure Notification process. This is a mandatory eBay-wide gate on every production keyset (subscribe to a user-data-deletion webhook, or apply for an exemption if the app never stores eBay user data), completely unrelated to whether the Cert ID string was copied correctly - a disabled keyset rejects OAuth regardless. Corrected `estimated-value.md`'s record in place (new "PRODUCTION KEYSET DISABLED" section replacing the now-wrong Cert-ID-truncation theory) rather than leaving the earlier wrong diagnosis standing uncorrected. `WebSearch`ed for the actual resolution since developer.ebay.com itself still 403s fetch tools: found the exemption path via eBay's own developer-community documentation (search-indexed excerpts, not independently verified) - toggle "Not persisting eBay data" to On on the Marketplace Account Deletion page, Confirm, pick the applicable exemption reason. This project's Browse API usage (read-only public listing search, no eBay account/user data ever touched) is exactly the case that exemption exists for. Not yet completed - waiting on the user to do this on eBay's side.

User also asked whether the 3 Amazon-capable providers found so far (2 built, Price Analytics flagged) are the only good options, framing it around "about 20 estimated value checks before running out of quota" - worth clarifying next turn that the *current* trial script queries every integrated provider together per title check, so its real bottleneck is whichever single 100/month pool empties first, not the sum of all providers' quotas - additional providers raise per-check confidence/coverage more than they raise total checks-per-month under the script's current all-providers-at-once design, unless it's changed to let the user spend from one provider at a time.

2026-09-17 (same day, follow-up): User gave 5 more instructions in one message, mid-research (interrupted a Price Analytics API-shape lookup to first answer a genuine clarification, then resumed). (1) Confirmed steelbook/collection handling matches original intent exactly, unprompted double-check: re-read `refresh.ts`/`searchTitle.ts` live rather than trusting memory across sessions and confirmed steelbooks are still searched for normally (the search string, gatherCandidates, everything runs the same), only poster/image-based confidence scoring is skipped for `is_collection`/`steelbook` rows (`allowImageMatch = !title.is_collection && !title.steelbook`) - nothing had drifted through any of the day's redesigns, no code change needed. (2) Built "Price Analytics" (3B Data) as the third Amazon provider - live-researched its actual submit-then-poll request/response shape via its RapidAPI playground (`POST /search-by-term` -> `job_id`, `GET /poll-job/{id}` -> `status`/`results`), but could only independently confirm the response shape for a GTIN search against its `idealo` source, not `source=amazon` specifically - built the parser defensively (accepts either an `offers` array or a single `price`/`currency` per result) and documented the real uncertainty in the file's own header rather than asserting a shape never actually seen. Flagged a genuine cost risk found along the way: each poll call almost certainly counts against the same 100/month quota as the initial search, so one Price Analytics "check" can cost several requests, not one - the polling cadence constants are an explicit best guess, unverified since the user asked not to spend quota testing it. (3) Rebuilt the whole Amazon-provider access pattern from "query all three every time" to a priority-ordered waterfall (`amazonProviders.ts`'s `searchAmazonWaterfall`) per the user's explicit request - added a shared `AmazonProviderResult` status type (`ok`/`quota_exceeded`/`error`) to `types.ts` and a `searchWithStatus()` variant on all three scrapers (RapidAPI's `429` treated as `quota_exceeded` for all three, since this feature's deliberately slow one-at-a-time call rate means a 429 in practice means the monthly cap, not a burst limit) so the waterfall can tell "this provider is spent for the month" apart from "this provider genuinely found nothing" and only fall through to the next provider on the former. Rewrote `trial-amazon-rapidapi.ts` to use the waterfall (one provider per title check now, not three). No persistent quota-tracking state was added - a manually-run trial tool re-discovering an already-exhausted provider costs at most one wasted call per run, judged an acceptable tradeoff over building a cache for it. (4) None of this was run for real, confirmed deliberately - the user explicitly said not to spend any quota yet, so `tsc --noEmit` was the only verification performed (clean on `packages/backend` and `scripts`). (5) Live-probed 10 more World-of-Books-style used-media resellers and international siblings (Better World Books, Decluttr, MusicMagpie retried, CeX, ThriftBooks, SecondSpin, Zoverstocks, momoxshop, Awesome Books) the same way as every other source this feature has tried - all either sat behind Cloudflare bot-protection or weren't running Shopify at all. No new sources found; documented as a real, checked negative result rather than silence.

Also corrected the eBay walkthrough for real this time: found and independently verified (via a direct fetch, not just search-indexed text) the actual working URL for eBay's exemption page - `https://edp.ebay.com/marketplace-account-deletion` - after several similar-looking candidate URLs from search results turned out to be dead ends (`developers.ebay.com` doesn't resolve at all; `developer.ebay.com`'s own docs page 403s like everything else on that domain). Rewrote `estimated-value.md`'s PRODUCTION KEYSET DISABLED section with the verified link and exact steps, and fixed a real formatting bug from the previous entry's edit - inserting that section had accidentally swallowed the "API compliance review" section's own header, leaving its body text orphaned onto the end of a different bullet; restored the missing header so both sections read correctly again.

2026-09-17 (same day, follow-up): User sent a screenshot of the actual eBay Application Keys page and pushed back on the previous entry's link: `edp.ebay.com/marketplace-account-deletion` just redirects to eBay's generic public guide, not an account-scoped action - asked how to navigate there starting from that exact screenshotted page instead. Re-read the screenshot itself rather than searching further: the disabled Production panel has its own inline message with two separate links ("...marketplace deletion/account closure notification process or apply for an exemption") - the fix was to point at the **"exemption"** link inline in that specific panel (reached only once logged into the developer account and viewing that specific disabled keyset), not the generic public URL or the neighbouring "notification process" link (which leads to the webhook-implementation guide instead, the wrong one for this project's read-only, no-eBay-user-data use case). Corrected `estimated-value.md` in place with this exact navigation path.

Then two more requests in the same message. (1) Asked to scan through previous research and compile a list of retailers already checked, to avoid re-checking the same ones in a future round - compiled every site name this feature has ever probed (working, dropped for bot-blocking, false-positive-Shopify, or other dead ends) plus every Amazon-provider candidate, into a new flat "ALREADY-CHECKED RETAILER LIST" section in `estimated-value.md`, placed right after the Retailers table specifically so a future "are there more sites like X" pass checks there first - the user's own recent "is there more like World of Books/JB Hi-Fi AU" question (answered earlier the same day in the "International-reseller research round 2" entry, 10 candidates checked, all dead ends) is exactly the kind of question this list exists to answer faster next time. (2) A new priority model for Amazon, in the user's own words: resellers run first as they already do; only if nothing scores above 0% confidence, or the user rejects every candidate shown, would Amazon even be considered - and even then, only as a per-title *choice* the user makes, informed by a quota progress bar ("so that the user can prioritize what products really need the amazon data and which would be fine with an estimated price"), never spent automatically. Declining the choice, or Amazon coming back with nothing accurate, both fall back to the existing random estimate logic. Built, but explicitly not run for real (instruction 3, unchanged from every earlier Amazon-related turn this session).

Design-wise, both of the user's named trigger conditions turned out to already converge on one existing state (`estimated_value_is_estimate = true AND estimated_value_is_manual = false` - the same state AUTO-ESTIMATED NOTE already defined on 2026-09-15), so no new intermediate screen or state was needed - the opt-in Amazon button was added directly to the existing `AutoEstimatedDetailScreen.tsx`. Refactored `refresh.ts` to export `scoreAllCandidates`, `cacheCandidateImage`, and a newly-extracted `resolveAndCachePosterUrl` (pulled out of `processTitle`'s own inline poster-resolution block) so the new route reuses the exact same confidence-scoring/image-caching pipeline every reseller candidate already goes through, rather than a second copy of it. Built `apps/web/src/app/api/estimated-value/auto-estimated/try-amazon/route.ts` (calls `searchAmazonWaterfall` for the one title, scores results, and either inserts a real `pending_value_review` row if anything scores above 0% or leaves the existing fallback estimate completely untouched otherwise) and a sibling `amazon-quota/route.ts` (read-only status for the progress bar).

For the quota bar itself, deliberately did not try to read RapidAPI's own "requests remaining" response headers - whether such a header even exists for any of these three providers has never been confirmed live, and checking would itself cost real quota the user has asked not to spend yet. Instead built a small self-tracked counter (`amazonQuota.ts`, new `amazon_provider_quota` table via `0025_add_amazon_provider_quota.sql`, private-project-only like `0021`/`0024`) that records exactly how many real requests this app's own code has made per provider per month - extended `AmazonProviderResult`/`AmazonWaterfallResult` with `requestsUsed` so `priceAnalytics.ts`'s submit-then-poll cost (1 + however many polls it actually took, tracked precisely rather than assumed) is reported accurately alongside the other two providers' flat 1-per-search cost. Usage is recorded only from the new route, never from the trial script (which still deliberately never touches Supabase) or from inside the waterfall/scrapers themselves (shared by both callers, neither of which has a Supabase client). `tsc --noEmit` clean across `packages/backend`, `apps/web`, `apps/mobile`, and `scripts`. Migration `0025` has not been applied to either live Supabase project yet - needs a fresh personal access token before the new route can work for real, flagged rather than assumed.

2026-09-17 (same day, follow-up): User provided a fresh Supabase personal access token and asked to apply `0025_add_amazon_provider_quota.sql` for real. Applied via the Management API against the private project only (`lpbsypfwhjluwuosrgnd` - same reasoning as `0021`/`0024`, this table is private-project-only), token used only inline in three curl calls (the migration itself, then two verification queries), never written to a file. Verified both the column shapes and the `(provider, month)` primary key constraint exist for real via `information_schema.columns`/`pg_constraint` queries rather than just trusting the empty-array success response. The "Try Amazon Search" route and quota progress bar are now fully live end-to-end against real data, though the user still hasn't asked to actually spend any quota.

Separately, asked to dispatch an agent to do retailer research rather than doing it inline - briefed a general-purpose agent with the full ALREADY-CHECKED RETAILER LIST (compiled the previous turn specifically to prevent this kind of duplicate work) plus explicit instructions to verify actual response *content* per candidate, not just HTTP status (the DiabolikDVD/ImportCDs false-positive lesson from earlier sessions), and to focus on Shopify-shaped stores (zero new scraper code needed) or bot-protection-free plain HTML. The agent live-probed roughly 25 candidates via direct `curl` and reported back: 8 confirmed genuinely-Shopify stores with real, verified search results (Second Sight Films, Powerhouse Films/Indicator, Radiance Films, Severin Films, Synapse Films, Via Vision Entertainment [+ Imprint Films], Cauldron Films, 101 Films Store), a longer dead-end list (Kino Lorber/Criterion/HMV UK Cloudflare-blocked, Arrow Films/Zavvi/Mondo Macabro/Terror Vision/Eureka Video not Shopify or not scrapable, Fopp a WordPress false positive, Half Price Books/WOW HD/Grainger Games/HMV Canada/Discover Books all dead for various reasons), and two honestly-flagged inconclusive candidates (Fun City Editions, Wonder Book - not bot-blocked but search-rendering unconfirmed, not claimed as either working or dead).

Asked the user via `AskUserQuestion` how much of this to build immediately (all 8 vs. just the highest-value pick vs. a hand-picked subset) rather than assuming - the user chose "integrate all 8 now." Built 8 new thin wrapper scrapers (`secondSightFilms.ts`, `powerhouseFilms.ts`, `radianceFilms.ts`, `severinFilms.ts`, `synapseFilms.ts`, `viaVision.ts`, `cauldronFilms.ts`, `films101Store.ts`), each reusing the existing shared `searchShopifyStore` exactly like Vinegar Syndrome/World of Books do - no new scraping logic needed, just a base URL, a source key, and a currency per site (GBP for the 4 UK labels, USD for Severin/Synapse/Cauldron, AUD for Via Vision). Wired all 8 into `refresh.ts`'s `INTERNATIONAL_SOURCES` (now 12 international sources plus 3 NZ ones, 15 total pending eBay). Flagged Via Vision as the one non-niche addition of the batch (AUD-priced, AU-shipped, expected to behave like `jb_hifi_au` rather than like the 7 niche boutique-label additions alongside it). Applied the same defensive merch-noise filter Vinegar Syndrome already uses to all 8 as a precaution, since none of their individual non-disc catalog compositions were separately confirmed. `tsc --noEmit` clean on `packages/backend`. Not live-tested against a real title afterward - unlike the Amazon providers, these are free/unauthenticated/unlimited public endpoints with no quota concern, so there was no reason to hold off the way Amazon's sources do; still, no real refresh run was actually triggered this turn, so the first real confirmation of these 8 sources' output will come from the next real scan or manual trigger. `estimated-value.md`'s Retailers table, ALREADY-CHECKED RETAILER LIST, and a new "2026-09-17 RESEARCH-AGENT ROUND" section all updated with the full result set, including the dead ends and inconclusives, not just the 8 that got built.

2026-09-17 (same day, follow-up): User scanned "Blade Runner: The Final Cut" and reported two real bugs in the scan/confirm flow (Best Match candidate showing an unrelated MCU-looking poster despite its own title reading "Blade Runner the Final Cut 2007"; the barcode's cut-specific title apparently being searched verbatim against OMDB/TMDb rather than split into a base film title + edition descriptor, likely why the match was poor) and asked a design question about analyzing the barcode's own product image to auto-detect disc format (DVD/Blu-ray/4K UHD/Steelbook) from the header band shown on the case. Answered the design question directly: recommended against training a custom image classifier (too much labeled-data collection for a personal project, format bands vary enough across studios to generalize poorly from a small set) in favor of a single vision-model API call (Claude/GPT-4V) per scan asking specifically about the visible format band - zero training data needed, and these bands are heavily standardized. Flagged that it should pre-fill and stay editable on ConfirmScreen rather than auto-commit, consistent with every other guessed/auto-filled field in this codebase. Dispatched a read-only Explore agent to investigate the two actual bugs (best-match image source, and where/whether the barcode title gets split before hitting OMDB/TMDb) before touching any code - not yet reported back as of this entry.

Separately, in the same message thread, fixed a real small bug the user reported on its own: Disc Count and Number of Special Features Discs on `ConfirmScreen.tsx` both used `keyboardType="number-pad"`, which only changes which on-screen keyboard is shown - it doesn't stop a pasted or autocorrected decimal point/symbol from landing in the field, so either could display something like "2.5" while `parseInt()` would silently truncate/reject it anyway at submit time. Added a small `digitsOnly()` helper (strips everything but `0-9`) and wired both fields' `onChangeText` through it, so the displayed value itself can never contain anything but digits, not just what eventually gets saved. `tsc --noEmit` clean on `apps/mobile`.

2026-09-17 (same day, follow-up): The Explore agent's Blade Runner investigation reported back with a precise root cause for both of the earlier bugs (best-match poster, bad title match): `scanResolver.ts` was searching OMDB with the barcode's full cleaned title ("Blade Runner the Final Cut") verbatim - OMDB indexes one entry per film, not one per re-release cut, so the real "Blade Runner" (1982) entry was never found, and the candidate list (poster included) came from whatever unrelated title OMDB's fuzzy search happened to surface for that literal string instead. No separate client-side image bug existed - confirmed no hardcoded fallback/default poster anywhere in the codebase; every render site already correctly shows "No image" rather than substituting a wrong one. The agent also found the fix's building blocks already half-built: `titleParsing.ts` already had `CUT_SUFFIX_WORDS`/`isCutVariantTitle` (unused elsewhere) for exactly this "Final Cut"/"Director's Cut" word list, just never wired into the search-query path.

Built on that directly: added `splitCutVariantTitle()` to `titleParsing.ts` (a new end-anchored variant of the same word list, since a cut/edition name is always trailing text - "Blade Runner the Final Cut", never mid-title - so anchoring to the end avoids ever touching an unrelated title that happens to contain one of these words) returning `{ baseTitle, releaseTitle }`. Wired it into both search paths per the agent's own suggested locations: `scanResolver.ts`'s automatic barcode search now searches `baseTitle` only, and `title-search/route.ts`'s manual "what's the title?" search does the same for a user-typed cut-variant title. On `ConfirmScreen.tsx`, `manualTitle` (the fallback title if no candidate gets picked) now pre-fills with just the base title, and `release_name`/its "matches title" checkbox now pre-fill from the full verbatim cut/edition text and uncheck automatically when one is found - directly closing the gap the user described ("information about cuts... should be put in the release name column"), previously manual-only since there was no reliable signal for it until now. `tsc --noEmit` clean across `packages/shared`, `packages/backend`, `apps/web`, `apps/mobile`. Not live-tested against a real re-scan of the Blade Runner disc.

Also answered the user's third question (analyzing the barcode's own product image to auto-detect disc format from the case's header band) as a design opinion, not yet built: recommended a single vision-model API call per scan over training a custom image classifier (no labeled-data collection needed, format bands are heavily standardized across studios), pre-filling the format field but staying user-editable, consistent with every other guessed field in this codebase.

2026-09-17 (same day, follow-up): Four more small requests in one message. (1) Merged the 3 separate per-provider quota bars on `AutoEstimatedDetailScreen.tsx` into a single combined bar (total used/total limit across all 3 providers), with a small text line underneath breaking down each provider's own count - the waterfall already spends from one provider at a time in a fixed order, so the single combined number is what actually matters for deciding whether a search is worth it. (2) Reject All on `PendingValueDetailScreen.tsx` now jumps straight to `AutoEstimatedDetailScreen` for the same title instead of returning to the Pending Value list - extended `pending/respond/route.ts`'s response to carry a `fallback` object (title/poster/estimated value) on an all-rejected outcome, so the mobile app doesn't need a second round trip to fetch what it already just computed; added a new `onRejectedAll` prop threaded through `App.tsx`. (3) The manual value field on `AutoEstimatedDetailScreen.tsx` now reformats to 2 decimal places (`50` -> `50.00`) on blur, not on every keystroke (which would fight the user mid-type). (4) The user's screenshot of eBay's actual "Alerts & Notifications" tab showed a different UI than what was described in the last walkthrough - no visible "Not persisting eBay data" toggle, but a locked/read-only panel (all fields grayed out) with an "Edit" button at the bottom. Corrected course rather than searching for more documentation a third time: told the user to click "Edit" first to unlock the panel, which should make the "Exempted from Marketplace Account Deletion" toggle interactive, flagged explicitly as a best reading of their actual screenshot rather than restated research, since the last two attempts at this exact flow were both wrong. `tsc --noEmit` clean on `apps/web` and `apps/mobile` for items 1-3; not live-tested against a real reject-all/manual-entry flow.

2026-09-17 (same day, follow-up): The user pointed out clicking "Edit" on eBay's actual Alerts & Notifications page only unlocked the endpoint text field, no exemption option ever appeared - and proposed logging into eBay themselves in a Playwright-controlled browser so Claude could see and interact with the real page directly, rather than continuing to guess from documentation. Confirmed the idea works but flagged the real security boundary first: the Playwright browser here is a separate, logged-out session with no access to the user's actual eBay cookies, and declined to accept eBay credentials through chat to log in directly - the user logged in themselves in that browser window instead, then handed off.

With real DOM access (not a screenshot), found the actual mechanism: the "marketplace deletion/account closure notification" and "exemption" links on the Keys page are literally the same URL (`/my/push/?env=production&index=0`) - the distinction described in earlier sessions' research (different links for different purposes) was wrong, not just imprecise. On the real Alerts & Notifications page, the "Exempted from Marketplace Account Deletion" switch itself was NOT actually disabled in the accessibility tree (only the endpoint/token fields and Send Test Notification were) - clicked it directly, which opened a "Please Note" confirmation dialog, clicked Confirm on that, and the real exemption form appeared: an "Exemption reason" radio group defaulting to "I do not persist eBay data" (already correctly pre-selected for this project's read-only usage) vs. an eBay-vendor-contract option, plus an optional notes field and a Submit button. Stopped there rather than clicking Submit - a real compliance declaration to a third party, left for the user to review and click themselves given eBay's own stated penalty warning for a wrong answer.

User submitted it and reported back a "new" Cert ID - checked against `scripts/.env`/`apps/web/.env.local` and found it was actually byte-for-byte identical to what was already stored (the exemption didn't rotate the cert, despite the user's impression it had) - no file update was actually needed, avoided guessing and instead verified directly. Then ran a real empirical test rather than re-eyeballing the credential's shape (the mistake that started this whole multi-session saga): a throwaway script (`__temp-ebay-oauth-check.ts`, deleted immediately after) requesting a real OAuth token via client-credentials grant - got a genuine `200` with a real access token (previously always `401 invalid_client`) - then a real Browse API search for "Thor Ragnarok blu-ray" against `EBAY_AU`, which returned genuine listings (a real $9.99 AUD Blu-ray listing with a real image and seller feedback score). eBay is now a fully live, confirmed-working source in this feature for the first time across the whole multi-session effort to get it there. `estimated-value.md` updated: the PRODUCTION KEYSET DISABLED section now has a RESOLVED subsection with the exact real steps (corrected from the wrong ones in earlier entries), the Retailers table row and working-source count updated to reflect eBay as genuinely working (15 sources total now).

Also answered two follow-up questions on the still-unbuilt vision-model format-detection idea: clarified that an Anthropic API key (console.anthropic.com) bills completely separately from a Claude Pro/Claude Code subscription - canceling one has zero effect on the other, addressing the user's stated worry about the feature breaking if they cancel Claude Pro. Advised against collecting training images - a pretrained vision model doesn't get fine-tuned through the public API for a task like this and already has ample general knowledge of standardized retail packaging; the right lever is prompt wording (describing what each format's banner looks like), not a training dataset. Dispatched a research agent (background, not yet reported back as of this entry) to compare current free-tier vision-capable APIs (Gemini, GPT-4o-mini, Groq, Mistral, OpenRouter, etc.) against Claude specifically for this task, per the user's explicit request for a pros/cons list before choosing a provider.

2026-09-17 (same day, follow-up): The eBay research agent reported back - recommended Gemini as the strongest genuinely-free option (no card, official TS SDK, strong at reading printed text/logos - a good match for reading a printed format banner), Groq as a free-but-"preview"-labelled backup, and OpenAI/OpenRouter/Cloudflare all ruled out for various reasons (no real ongoing free tier, a rotating free model lineup, or an unproven fit for custom JSON prompts respectively). The user picked Gemini and provided a real API key directly in chat.

Verified the key live before building anything, rather than trusting its format - it doesn't look like a standard `AIzaSy...` Google API key (starts `AQ.` instead), which given this session's earlier wrong Cert-ID-format guess with eBay was reason enough to actually test it instead of flagging a shape mismatch again. `curl`'d Gemini's own `/models` list endpoint directly: it's real and valid, and exposed a full model catalog running well past this assistant's own January 2026 knowledge cutoff (`gemini-3.5-flash-lite`, `gemini-3.8-flash`, etc.) - picked `gemini-flash-lite-latest`, Google's own always-current alias for its lightest/highest-free-quota tier, matching the user's explicit request for "a model that will give us the amount of prompts that we will need" over the most capable one. Live-tested the full technical mechanism end-to-end via more `curl` calls (a real JPEG, base64-inlined, sent with `generationConfig.responseMimeType: "application/json"` + a `responseSchema`) before writing any code - confirmed this returns clean, unfenced JSON (a plain-text-prompt version of the same test came back wrapped in a ```json code fence instead, which would have needed extra unwrapping).

Built `packages/backend/src/formatVision.ts` (`detectFormatFromImage`), using Gemini's plain REST `generateContent` endpoint via `fetch` - no `@google/genai` dependency added, matching the same "plain fetch over an SDK" convention every other external API in this codebase already follows (ebay.ts, omdb.ts, tmdb.ts). Wired into `scanResolver.ts` as strictly the LAST resort: it only ever runs when `extractFormatHint` (the existing text-based guess) already found nothing, per the user's own explicit instruction that text is always more reliable than a model guessing from a photo - added a `visionFormatGuess` field to `resolved_candidates` for this. On `ConfirmScreen.tsx`, `format` and `steelbook` now fall back to this guess (after draft/existingMatch/text-hint, before the plain "DVD" default) - still a plain editable field either way, no different from any other guessed value already in this screen. Confirmed directly with the user this doesn't need a custom training dataset - a pretrained vision-language model already has ample exposure to standardized retail packaging, so the real lever is prompt wording (describing what each banner looks like), not collecting images.

Mid-build, the user added two more real details about multi-disc banners from direct experience: a 4K UHD banner sometimes also says "+ BLU-RAY" (a second, Blu-ray special-features disc) or "+ BLU-RAY + BONUS DISC" (a third disc, also assumed Blu-ray, never seen otherwise) - and separately, a plain Blu-ray banner can say "+ DVD" for its own bonus disc (a different assumed format than the 4K UHD case, not the same one). Deliberately kept the "what format is an unlabeled bonus disc" assumption out of the model prompt entirely - the model's `extraDiscs` field only ever reports how many extra items the banner lists ("NONE"/"ONE_EXTRA"/"TWO_EXTRA"), never what they are, and a new `deriveDiscConfigFromExtraDiscs` function in `ConfirmScreen.tsx` applies the user's stated real-world rule (Blu-ray alongside a 4K UHD primary, DVD alongside a Blu-ray primary) as plain, auditable app code - easy to correct or extend later if a different real combination ever turns up, rather than needing to reverse-engineer an opaque prompt. This pre-fills disc count, Special Features, special-features disc count, and special-features disc format together whenever a multi-disc banner is detected - all still plain editable fields, per the user's own explicit instruction ("fill out the entries in the form but allow them to be editable by the user in case of errors").

Added `GEMINI_API_KEY` to `scripts/.env.example` (documented), `scripts/.env`, and `apps/web/.env.local` (the real key, gitignored, never committed) - not a private-project-only feature (unlike Estimated Value), so no public-repo exclusion needed, same as OMDB/TMDb's existing keys. `tsc --noEmit` clean across `packages/backend`, `apps/mobile`, `apps/web`. Not live-tested against a real barcode-scanned disc photo yet - the generic image+JSON mechanism was proven live via curl, but real accuracy on an actual disc case (and the multi-disc banner logic specifically) will only be confirmed the next time a barcode scan actually reaches this code path.

2026-09-17 (same day, follow-up): The user rescanned the actual Blade Runner disc and reported every fix from the last several entries was apparently still broken (MCU poster, full-cut-text search, no vision format detection) despite all of it being built and typechecking clean. Root-caused directly rather than re-guessing at the code: `netstat`/`Get-Process` showed the `next dev` server on port 3001 had been running since 9:14am, hours before nearly all of today's changes - Expo's own reload only refreshes the mobile JS bundle, never the backend server where `scanResolver.ts`/`formatVision.ts` actually execute. Killed and restarted it, found the exact `pending_scans` row for this disc still sitting "resolved" with the stale data, reset it to "pending" so the fixed server's own 15-second auto-resolve poller (`instrumentation.ts`) picked it up fresh, and verified the result directly via Supabase queries rather than trusting the fix blind: the candidate list now correctly includes the real 1982 "Blade Runner" (`tt0083658`) with its own real poster. Also downloaded and looked at the actual bad OMDB poster directly (via the Read tool's image support) to settle what had been an assumption until now - confirmed it's genuinely a mismatched podcast-cover-art image OMDB itself serves for an obscure alternate IMDb entry ("Blade Runner: The Final Cut (2007)," `tt24742930`), not a client-side image-selection bug in this codebase at all; the title-split fix means that bad entry no longer dominates the results even though the underlying OMDB data glitch itself is outside this project's control.

Found one real, separate limitation while investigating: this exact barcode listing's own UPCitemdb title text literally contains the word "Dvd" ("Blade Runner The Final Cut (uk Import) Dvd"), despite the real disc being 4K UHD - meaning the text-hint-wins-over-vision rule (built exactly as the user specified) produces a wrong answer for this one mislabeled listing. Flagged rather than silently overridden. The user's follow-up confirmed they wanted this fixed, so refined the rule rather than reversing it outright: `extractFormatHint`'s "DVD" result specifically (its own generic fallback, only returned once its 4K/Blu-ray/VHS patterns have all failed to match) is now treated as non-specific and cross-checked against a real product photo when one exists, while every other, more specific text hint (Blu-Ray/4K UHD/VHS) still always wins outright, unchanged. Updated both `scanResolver.ts`'s trigger condition and `ConfirmScreen.tsx`'s precedence logic to agree on this exact same distinction.

Root-caused a second real bug the same way: after fixing the above and restarting again, `visionFormatGuess` still came back `null`. Checked `apps/web/.env.local` directly rather than assuming the fix was live - `GEMINI_API_KEY` had only ever been added to `scripts/.env`, never to the file the actual running server reads from. Added it there too, restarted a third time, and got a real result this time: `{"format":"4K UHD Blu-Ray","steelbook":false,"extraDiscs":"TWO_EXTRA"}` - a genuine, correct read of the case's real 4K UHD banner.

User then pointed out the real banner actually reads "4K UHD + Blu-Ray + Digital Download," and a digital download code isn't a physical disc - `extraDiscs` should have been `ONE_EXTRA`, not `TWO_EXTRA`. Updated `formatVision.ts`'s prompt to explicitly exclude "Digital"/"Digital Download"/"Digital Copy" wording from the extra-discs count. Restarted, reset, re-tested - still got `TWO_EXTRA`. Rather than assume the prompt update wasn't live again, downloaded the real product image directly (`Read` tool's image support) to see the actual banner with certainty: it does say "4K ULTRA HD + BLU-RAY + DIGITAL DOWNLOAD," confirming the prompt's own logic was correct. Ran the *exact* current prompt against the *exact* same image via a direct one-off `curl` call outside the app entirely - got `"extraDiscs": "ONE_EXTRA"`, the right answer, from the identical inputs that had just produced `TWO_EXTRA` through the real pipeline twice. This ruled out a deployment/staleness problem for the first time all session and pointed to genuine model non-determinism instead - Gemini's default sampling temperature. Added `temperature: 0` to the request's `generationConfig`, restarted once more, and got a clean, correct `ONE_EXTRA` through the real app pipeline this time.

Also downloading the real image directly disproved the assumption behind the user's separate "big grey borders on the left and right" report - the source photo itself has no padding at all (a clean full-bleed portrait cover). Had already built a Jimp `autocrop()`-based border-trimming utility (`imageCrop.ts`, using the newly-added `@jimp/plugin-crop`) before this was confirmed, on the reasonable assumption the source image had baked-in padding like some retail-CDN photos do - removed it once the real cause was found to be a display bug instead: `ConfirmScreen.tsx`'s "Your scanned item"/"Existing entry" images used a fixed short `height: 220` box with `resizeMode="contain"`, which pillarboxes a tall portrait cover photo and exposes the box's own dark background (`#18181b` - dark enough to read as "grey" at a glance) on both sides. Replaced the "Your scanned item" image's style with a new `scannedItemImage` using `aspectRatio: 2/3` (a typical disc-case proportion) instead of a fixed height, and switched both it and "Existing entry"'s image to the same `resizeMode` convention already used everywhere else in this file. Uninstalled the now-unused `@jimp/plugin-crop` dependency rather than leaving speculative, unused code in the tree - a real per-source-image cropping need hasn't actually been confirmed anywhere yet, and the utility is cheap to rebuild if a genuinely padded photo ever does turn up.

Separately, updated both OMDB-candidate poster-placeholder spots on `ConfirmScreen.tsx` (the main/Extras candidate list and the "Matched by cover photo" auto-match card) from "No image" to "No poster image found," per the user's own request to make the empty-placeholder text less ambiguous.

Finally, the user asked to push to GitHub, noting it hadn't been updated "for a while" - checked and confirmed: the last commit predated this entire session's work (retail sourcing, Amazon waterfall, eBay, vision detection, everything). Found and gitignored a `.playwright-mcp/` scratch directory (browser snapshots/console logs from the live eBay walkthrough) before staging, confirmed no `.env`/secret files were staged, and committed+pushed the whole session's work to the private repo's `origin/main` as one consolidated commit (72 files, ~3500 insertions). The public repo's GitHub URL/local checkout location wasn't available this session - still needs the user to provide it before `sanitize-public-repo.ts` can be run and that half pushed too.

The user also separately asked for barcode-scanned product images to be uploaded into Supabase Storage and to take priority over whatever image the Estimated Value price-scraping workflow would otherwise cache for the same title - a real, still-open design question (which column, which bucket, exactly how "priority" should be expressed against `resolveAndCachePosterUrl`'s existing `poster_image_path` guard) that hasn't been built yet, deliberately not guessed at given it touches a schema change across both live Supabase projects and this session already had two real wrong-guess lessons (the eBay Cert ID shape, and the border-cropping assumption) from acting on an assumption instead of checking first.

2026-09-18: Two more requests. (1) On the similar-entry (Overwrite/Is-a-new-entry/Reject) screen, show each candidate's `release_name` alongside its title, since two rows sharing the same base title are often actually different releases (a plain DVD vs. a Special Edition) that the title text alone can't distinguish. Added `release_name` to `find-existing/route.ts`'s `SELECT_COLUMNS`/`ExistingTitleRow`, the mobile-side `ExistingTitleCandidate` type, and all three render spots in `ConfirmScreen.tsx` (the single-match banner, the multi-candidate picker list, and the chosen-candidate detail block) plus the one other place in the same file that manually constructs an `ExistingTitleCandidate` object (the exact-barcode-match fast path) - `tsc --noEmit` caught that second spot immediately as a missing-property error, confirming it needed the same fix.

(2) The user scanned "The Day the Earth Stood Still" and found no Rotten Tomatoes link saved despite RT genuinely having a page/score for it - asked to investigate and prevent recurrence. Root-caused directly: `rottenTomatoes.ts` guesses a URL slug from the title text (no real RT search is available - their robots.txt disallows `/search`, and this project has never scraped against a site's own robots.txt) and only accepts the guess if the resulting page's JSON-LD matches both title and score. For this film, RT's real slug is `1005371-day_the_earth_stood_still` (a numeric-ID prefix) - confirmed live via `curl` that the guessed slug 404s, and found two much older Sheet-imported rows for the same 1951 film that already carry the correct link (evidently typed in by hand, long before this automation existed). This is a real, structural limitation of guess-from-title-text, not a one-off bug - the code behaved exactly as designed ("wrong link is worse than no link").

Built `scripts/src/find-missing-rotten-tomatoes.ts` (read-only audit, checks every title with no saved RT link against OMDB's own `Ratings` array) to surface this gap for manual review going forward, since no better automated fix exists within this project's scraping-ethics constraints. Ran it for real against the live collection: **47 titles** have a confirmed Rotten Tomatoes score but no saved link - a real, ongoing gap across the whole collection (old Sheet-import-era rows and freshly-scanned ones alike), not an isolated case, as initially suspected before running the audit. Documented the full finding, root cause, and the "run this occasionally, paste the correct URL into the Sheet by hand" workflow in `barcode-scanning-pipeline.md`. Gave the user the one already-known correct URL for "The Day the Earth Stood Still" directly rather than writing it into the Sheet myself, consistent with treating the Sheet as the user's own document to edit. `tsc --noEmit` clean on `scripts`.

2026-09-18 (same day, follow-up): User asked to log the Rotten Tomatoes link-gap as a known bug to fix later rather than build a real fix now - added to `Claude/To Do list.md`'s BACKLOG with the audit script command and a pointer to the full write-up. Also asked to be walked through the earlier-proposed Supabase case-image plan - explained it, and the user corrected a real misunderstanding in it: they want BOTH poster images (OMDB/TMDb, for future Movie/TV Pages) and product/case images (barcode scans or accepted Estimated Value candidates, for future DVD Pages) kept side by side, not one replacing the other - pointed to `WEB APP DESIGN.md`'s own DVD Pages section, which already documents this exact split. The "priority" they actually meant was narrower than my first draft assumed: only between the two SOURCES of the case image (barcode scan vs. Estimated Value candidate), never between the case image and the poster.

Revised and built the corrected plan: `0026_add_case_image_path.sql` adds `titles.case_image_path` (a Storage path, same convention as `poster_image_path`) and a new `case-images` bucket, kept deliberately separate from Estimated Value's own `retail-product-images` bucket since that one lives entirely inside the private-only `estimatedValue/` directory excluded from the public repo, while this needs to work in both builds. New general-purpose `caseImageStorage.ts` (fetch-and-upload, signed URLs) mirrors `estimatedValue/storage.ts`'s shape without depending on it. Two write paths feed the same column with priority expressed as a plain "don't overwrite if already set" guard, the same idempotent pattern `poster_image_path` itself already uses - no comparison logic needed: `scan/confirm/route.ts` writes unconditionally on every confirm (a fresh photo of the real physical copy always wins), while `pending/respond/route.ts`'s candidate-acceptance path only writes when `case_image_path` is still null, i.e. only ever reaches a title with no barcode photo at all (typically pre-barcode-pipeline). The accepted-candidate path is a genuine cross-bucket copy (resolve a signed URL against `retail-product-images`, fetch through it, re-upload into `case-images`), since Storage's own `copy()` only works within one bucket. Added `case_image_path` to the shared `Title` type and to `titleParsing.ts`'s "no Sheet column" list (a Storage path is DB-only, same as `poster_image_path`, never something the user edits by hand in the Sheet). `tsc --noEmit` clean across `packages/shared`, `packages/backend`, `apps/web`, `apps/mobile`.

User then provided a fresh Supabase personal access token and confirmed they wanted this integrated for real. Applied the migration to BOTH live projects via the Management API (not private-only, unlike 0021/0024/0025) - token used only inline in three curl calls, never persisted. Verified genuinely live on both (not just trusting the empty-array success response) via a direct `information_schema.columns`/`storage.buckets` query against each project - confirmed the column and bucket both exist on the private (`lpbsypfwhjluwuosrgnd`) and public (`azfpstsvngohgktjilms`) projects alike.

Also fixed a real regression the user caught: the earlier "fix" for the Blade Runner scanned-item image's grey borders (a hardcoded `aspectRatio: 2/3`, replacing an even earlier hardcoded fixed height) just moved the same letterboxing problem to the top/bottom instead of the left/right, since 2:3 isn't actually this photo's real shape either. Replaced both guesses with `Image.getSize()` reading the photo's own real dimensions and setting the box's aspect ratio to match exactly - the only approach that can't be wrong for a given image, since it isn't a guess at all. 2:3 remains only as the initial/fallback value before the real size loads. `tsc --noEmit` clean on `apps/mobile`.

2026-09-18 (same day, follow-up): The user found a Supabase Security Advisor warning ("RLS Disabled in Public," critical) for `amazon_provider_quota` - a real gap this session caused: `0025_add_amazon_provider_quota.sql` created the table without ever enabling Row Level Security, unlike every other table in this project. Fixed with `0027_enable_rls_amazon_provider_quota.sql` - `enable row level security` with deliberately NO read/write policies added, unlike `title_price_observations`/`pending_value_review` (which do get a public-read policy for a future public-facing web app): this table is pure internal RapidAPI-quota bookkeeping with no legitimate anon/authenticated use ever, so a full lockdown (service role still bypasses RLS regardless, anon/authenticated get denied by default with zero policies) is the correct shape here, not the public-read pattern the other Estimated Value tables use. Applied to the private project only (confirmed the table doesn't exist on the public project at all, via `to_regclass`, since 0025 itself was never applied there) and verified genuinely enabled afterward via `pg_class.relrowsecurity`, not just trusting the success response. Added to `sanitize-public-repo.ts`'s exclusion list alongside 0025. `tsc --noEmit` clean on `scripts`.

2026-09-18 (same day, follow-up): The user reported a real consequence of yesterday's mislabeled-listing find: after submitting the Blade Runner scan, the similar-entry (Overwrite/New Entry/Reject) screen showed two candidates, both format "DVD" - a box-set member and what they suspected might secretly be the 4K version. Since neither looked like a 4K match, they chose "Is a new entry," then later confirmed via the Sheet that the "DVD" one really was the 4K UHD copy - creating an accidental duplicate.

Root-caused with real data, not just theory: queried every Blade Runner row directly and confirmed the real 4K duplicate (`7b43234c...`) genuinely has `format = "4K UHD Blu-Ray"` in the database (not "DVD" at all) and shares the same real `imdb_page` (tt0083658) as the newly-created row - meaning it DID match via the shared-imdb-id path in `find-existing/route.ts`, but was then filtered back OUT by the route's own format-hint narrowing step. That step re-derives `formatHint` via `extractFormatHint(upcText)` from the raw barcode listing text server-side - and this exact listing's own title text says "...Dvd" (the same mislabeling already found and worked around for the vision-detection feature), so the narrowing kept only the box-set's genuinely-DVD member rows and silently hid the one candidate that actually mattered. ConfirmScreen had already correctly resolved the real format via the vision fallback by this point, but that corrected value was never passed to this endpoint at all - the client only ever sent the raw upcText, and the server re-derived its own (wrong) guess from it independently, disconnected from what the screen was actually showing the user.

Fixed by passing the client's own already-resolved `format`/`discCount` state through as explicit `formatOverride`/`discCountOverride` params (`scanApi.ts`'s `findExistingTitle`, `find-existing/route.ts`), preferred over re-deriving from raw text - falling back to the old text-derived hint only when a client doesn't send an override at all. `tsc --noEmit` clean on `apps/web`/`apps/mobile`. Did not touch the two now-duplicated Blade Runner rows themselves (`103eeec2...` and `7b43234c...`) - flagged to the user as a real cleanup step but left for them to decide/action, consistent with treating collection data edits as theirs to make.

2026-09-18 (same day, follow-up): (1) Deleted the old duplicate "Blade Runner The Final Cut" row (`7b43234c...`) the user asked to remove - a one-off script (deleted immediately after) removed it from both Supabase and the Sheet, hitting one real bug along the way: a naive `String.fromCharCode(65 + index)` column-letter helper broke on this Sheet's "Unique Identifier" column being past column Z (index 26) - fixed by reusing the project's own existing `columnLetter` helper instead of reinventing one. (2) The user clarified a standing cataloguing rule: a specific CUT of a film ("the Final Cut," "Director's Cut," ...) should be appended to the end of the catalogued `title` itself, never moved into `release_name` the way a packaging/special edition is - reversing part of the 2026-09-17 fix, which had prefilled release_name from the detected cut. Extended `splitCutVariantTitle` (titleParsing.ts) with a new `cutSuffix` field (the matched cut phrase alone, verbatim-cased) alongside its existing `baseTitle`/`releaseTitle`; `scan/confirm/route.ts` now appends it (`"<base>: <cut>"`) to whichever title it ends up with, real-candidate or fully-manual alike; reverted release_name's auto-prefill back to manual-only. `tsc --noEmit` clean on `packages/shared`/`apps/web`/`apps/mobile`.

(3) The user then spec'd out an offline-mode feature across several rapid messages: a warning banner when offline, scanning/manual entry still working without internet, Confirm being blocked from actually reaching the server while offline (saved locally on-device instead, with an explanatory message), automatic resubmission once connectivity returns (running the full normal pipeline including Estimated Value), and a real duplicate found during that automatic resync being flagged for the user to resolve later rather than shown live. Installed `@react-native-async-storage/async-storage` and `@react-native-community/netinfo` as a first step - the actual queueing/sync/duplicate-flagging implementation was not yet built when this session hit its context limit. Also owed: a paragraph giving an opinion on using the phone camera to read a disc case's front cover directly (beyond just the barcode-driven product-photo lookup already built) - not yet written.

2026-09-18 (same day, follow-up, continuing after the session limit was hit): Picked up exactly where the previous session left off. (1) Logged the "scan the cover" idea to `Claude/To Do list.md`'s BACKLOG as explicitly deferred until after Collection/TV series integration, per the user's own sequencing - captured their clarification that scanning the cover and scanning the barcode are independent, not mutually exclusive (either or both), and the concrete field list they expect from a single cover photo alone (title, format, disc count, edition, product image, special-features disc format via 4K UHD-style headers, franchise, an estimated price if a tag is visibly still attached, and rating). Gave the camera-recognition opinion itself in the previous message (favorable - the Gemini format-vision work already proved the core approach, main cost is UX/framing testing, not technical risk).

(2) Built the full offline-mode feature the user spec'd out across several messages: a persistent warning banner (`OfflineBanner.tsx`, on ScannerScreen and both ConfirmScreen states) shown whenever there's no real internet access - deliberately checking `NetInfo`'s `isInternetReachable`, not just whether a network interface is up, since the phone and this app's own LAN-hosted backend can stay reachable to each other even when the wider internet is down (`network.ts`). Confirm is now offline-aware at both points that need the network before the real write (the interactive similar-entry check, and the write itself) - offline, it skips straight to saving the fully-built submission on-device (`offlineQueue.ts`, AsyncStorage-backed) instead of failing with a network error, with a clear message that it hasn't been added to the database yet. An `App.tsx`-level listener fires the moment the connection comes back (`subscribeToReconnect`, added to `network.ts` - fires only on a genuine offline->online transition, never on an online->online network-type change like WiFi to cellular) and attempts every queued item for real: the exact same `findExistingTitle` check a live submission would have run, then either submits immediately (no match) or - per the user's explicit instruction - flags the item as `needs_review` with the match data attached rather than resolving it automatically, since an unattended background sync has no way to make that Overwrite/New Entry/Reject call itself. A new `OfflineQueueScreen.tsx` (reached via a badge-counted "Offline Queue (N)" button on Pending Scans, shown only when the count is nonzero) lists queued/needs_review/error items and lets the user make that deferred decision by hand, reusing `confirmScan` directly rather than routing back through ConfirmScreen's full form (all the field data was already captured at queue time). Installed `@react-native-async-storage/async-storage`/`@react-native-community/netinfo` in the previous session; this one wrote all the actual logic. `tsc --noEmit` clean across `packages/shared`, `packages/backend`, `apps/web`, `apps/mobile`. Not yet tested against a real lost-connection scenario on a device - the mechanism is built and typechecks, but genuine offline/reconnect behavior (and AsyncStorage persisting across an app restart) has only been reasoned through, not observed live.

2026-09-18 (same day, follow-up, new session): User confirmed the case-image-priority item from the previous session's final paragraph had actually already been fully resolved and built later that same day (a self-correction of a misreading, not a real open item - nothing in `To Do list.md` needed removing). Then asked to start Phase 1's TV scanning support, with a detailed set of field-semantics rules given up front (Season No./Part of a Season No./Episode Count meanings, TV running_time defaulting to n/a, Director meaning Creator/Head-Writer for TV, IMDb page/release_date always reflecting the whole show not one season). Spent most of the session on real clarifying questions before writing code, per the user's own standing instruction - each one grounded in either the actual live pipeline (a background Explore agent traced the current movie-only scanning code first) or a live data audit, not guesses:
- IMDb's own robots.txt disallows scraping title pages site-wide (checked directly) - ruled out as a `movie_or_tv` detection source, confirming the type field must stay always-visible/editable rather than hidden-until-no-match like Rating/Studio.
- Built `scripts/src/audit-movie-or-tv-values.ts` (read-only, kept as a reusable tool) before trusting "pull the real dropdown vocabulary from live data" - came back clean (0 stale, 0 near-duplicates), 14 real categories.
- A deeper season_no/running_time/director audit surfaced real, undocumented conventions already live in the collection: "various"/"assorted" both in use (user picked "Assorted" going forward), a new "All" value (complete-series box sets) and "Specials" value (unnumbered specials) neither one in the original spec, ad hoc "1 and 2"/"6-7" phrasing needing conversion to comma-lists, two Miss Marple rows mistyped as "Movie" with a stray "Unknown" season, and a Star Trek TOS 79-disc box set genuinely repurposing `part_of_season_no` as a disc/volume number (kept as a documented exception). A separate discovery: the "long IG" legacy runtime value the user asked to clear wasn't TV-specific at all - it turned out to be a general ~280-row placeholder used across TV box sets AND plain movie collections alike; cleared everywhere since it already parsed to null regardless (pure Sheet-text cleanup, no DB effect).
- Published a "TV Runtime Review" Artifact (https://claude.ai/artifact/D2HLVuBusUD2j11yzJLSke) surfacing all 162 TV-typed rows with an existing runtime, since the user wanted to decide those by hand rather than have a blanket rule applied - explicitly a read-only report (no form/save/database capability), confirmed later in the session via re-reading the live page and checking its (empty) comment threads, after the user asked to double-check nothing "submitted" to it could be lost the way temporary pages have failed before.
- Discovered mid-build that "extend TMDb to cover TV" - filed at first as a deferred nice-to-have - was actually a hard blocker: the confirm route's existing hard requirement (any OMDb-matched entry must resolve to a real TMDb id) would reject every TV confirmation outright, since `lookupTmdbFields` only ever checked `movie_results`. Fixed properly (also checks `tv_results`, added real `/tv/{id}` + `/tv/{id}/content_ratings` support to `fetchTmdbFieldsById`), which needed its own real decision (asked, not assumed): a new `tmdb_media_type` column (`0028_add_tmdb_media_type.sql`) so the ~5-month refresh job knows which endpoint a stored id needs, rather than re-deriving it from `movie_or_tv` (which can drift after the fact) - backfilled to "movie" for every pre-existing row per the user's instruction, since TV lookup support didn't exist before this session.

Built (all typecheck clean across `shared`/`backend`/`web`/`mobile`/`scripts`): `packages/shared/src/titleParsing.ts`'s `SEASON_NO_ALIASES`/`normalizeSeasonNo` (wired into `parseSheetRowToTitle`, so future syncs auto-normalize); `scripts/src/backfill-season-no-normalization.ts` (one-time, already run - various->Assorted, casing, mis-punctuated lists, the 3 bare "Multiple" rows resolved by title wording, the Miss Marple reclassification); `packages/backend/src/tmdb.ts`'s TV-aware `findTmdbIdByImdbId`/`fetchTmdbFieldsById`/`lookupTmdbFields`/`refreshTmdbFields`; `packages/shared/src/omdb.ts`'s `Writer`/`seriesID` fields; `scan/confirm/route.ts`'s episode->series resolution (release_date/imdb_page always reflect the whole show) and Director-vs-Creator/Writer switch (scoped to whole-season/show-level matches only, via OMDb Type - a single-serial/episode match keeps using Director, matching the 353-of-469 real rows already using that pattern); `find-existing/route.ts`/`scanApi.ts` now carry `movie_or_tv`/season/episode fields so a rescan pre-fills the already-catalogued type instead of re-guessing; mobile `fieldOptions.ts` gained a live `movieOrTv` list; `ConfirmScreen.tsx` got a real, always-visible "Movie or TV" field (reused the existing `SingleSelectChips` component - a closed choice, no free text, per the user's explicit ask) plus Season No./Part of a Season No./Episode Count fields gated on the TV-ish subset of `movie_or_tv` values (`SEASON_FIELDS_MOVIE_OR_TV_VALUES` - excludes TV Movie, which real data confirmed essentially never has a season_no set).

Applied `0028` to both live Supabase projects with a fresh token (verified via `information_schema`/a direct count query, not just trusting the success response - private project's 9 pre-existing `tmdb_id` rows all correctly got `tmdb_media_type='movie'`; public project has 0, expected for a barely-scanned demo dataset). Wrote real design decisions into `Claude/RESOURCES.md` (a full "TV field semantics refined" note - Assorted/All/Specials, the Star Trek exception, Director-vs-Creator scope, release-date/IMDb-page rules) and `Claude/TECH STACK AND ARCHITECTURE/barcode-review-screen-fields.md` (a full new "TV SCANNING" section) as the decisions were made, not after the fact.

User then asked to verify two things rather than just take the summary on faith: (1) that the season/episode fields really do appear/disappear correctly for a fully-manual entry, not just a matched one - traced the actual JSX nesting to confirm the Type/season block sits at the top level, not inside the `selected.size === 0` conditional, and attempted a live check by seeding a throwaway `pending_scans` row and trying `expo start --web` - stopped and asked first rather than silently installing the missing `react-native-web` dependency the web target needs, cleaned up the test row either way; (2) that the TV Runtime Review artifact couldn't lose anything "submitted" to it - confirmed by re-reading the live published page and checking its comment threads (both clean), since it's genuinely read-only with no save mechanism.

User then asked to start both dev servers for real device testing. Found and fixed a real mismatch while doing it: `npm run dev` in `apps/web` defaults to port 3000, but `apps/mobile/.env`'s `EXPO_PUBLIC_SCAN_API_URL` is configured for port 3001 - started `next dev -p 3001` explicitly instead (killed an orphaned process from a first stopped attempt along the way). Cross-checked the machine's real Wi-Fi IP via `ipconfig` against what was already in `.env` (`192.168.1.71`) - confirmed correct, so the only real fix needed was the port. Expo's own QR code didn't render through the background task's piped log capture; gave the user the manual `exp://192.168.1.71:8081` entry as a fallback.

While the user reviewed the TV Runtime Review page for real, they flagged four rows (`Zorro Rides Again`, `Batman and Robin`, `Dick Tracy`, `Flash Gordon Conquers the Universe`) as looking like classic theatrical serials rather than ordinary TV seasons, with different rules to come later - confirmed via live OMDb lookups rather than assumed: all four are real Republic/Columbia/Universal serials, and three already store OMDb's own full-serial runtime total exactly (`Batman and Robin` 263 min/15 episodes, `Flash Gordon Conquers the Universe` 195 min/12 episodes, `Zorro Rides Again` 212 min matching OMDb's total) - `Dick Tracy` (1937) is shaped differently (no season_no/episode_count at all, a single 70-min entry matching OMDb's own Runtime exactly, suggesting a condensed "feature" re-edit rather than a chapter set). Left all four untouched pending the user's serial-specific rules. Applied the rest of a numbered list of per-title decisions from the runtime review: fixed "The Compelete Claymation Minifigger Collection" -> "The Complete Claymation Minifigger Collection" (title typo, "Minifigger" deliberately left as-is), cleared `running_time_mins` on 11 rows (`Classic TV Comedians`, `Inspector Morse Collection`, the Claymation Minifigger row, `The Three Stooges Volume 1`, `The Day of the Triffids`, `The Nazis: A Warning from History`, and all 5 `The Agatha Christie Hour: ...` rows), applied to the Sheet then synced and verified in Supabase. Documented the refined per-category runtime rule (TV Series off by default/case-by-case exceptions, TV Mini-Series on by default/case-by-case exceptions, TV Movie/TV Special/TV Episode always on) and the still-open SERIALS question in `Claude/RESOURCES.md`.

Not yet done: pushing any of this session's work to either GitHub repo.

2026-09-18 (same day, follow-up): User first asked whether the four flagged titles were actually verified as serials rather than just assumed - re-stated the same live OMDb findings already gathered, more explicitly this time (a table of title/match/runtime for each), confirming the earlier check had in fact been real.

User then gave the actual serial rules: serials get their own `movie_or_tv` value, literally "Serial" (any "TV Serial"/"Serial TV" variant normalized to it) - always `season_no` "1", `episode_count` counted the same way TV Series counts it, and always keep a real runtime (unlike TV Series's default n/a), since online databases present a serial in "movie" shape with no episode breakdown and its story definitively concludes, unlike an open-ended show. Also flagged a rare, deliberately-deferred complexity: a serial can sometimes nest inside a TV show (a season with multiple serials, a serial spanning multiple episodes) - Doctor Who being the extreme case of this - which the user tends to ignore for now and will give more specific logic for later if it's ever actually needed.

Checked the live Sheet before changing anything and found a real, previously-invisible bug: `MOVIE_OR_TV_ALIASES` had `"tv serial": "TV Series"` - the user had ALREADY typed "TV Serial" in the Sheet for `Dick Tracy` and both `Zorro Rides Again` rows, and every sync since had been silently collapsing that distinction away without them ever knowing. Fixed (`"tv serial"`/`"serial tv"`/`"tv-serial"`/`"serial"`/`"serials"` all now map to "Serial").

Reclassifying the five confirmed serial rows by title text then caused a second, self-inflicted bug: `"Batman and Robin"` and `"Dick Tracy"` both collided with an unrelated film sharing the exact same title (the 1997 Joel Schumacher `Batman and Robin` feature, and the 1945 RKO `Dick Tracy` feature - both real, different films from unrelated years) - both got briefly mislabeled "Serial"/season_no "1" before being caught. Caught it by directly investigating the two duplicate-titled row pairs (not by luck) before syncing, reverted both wrong rows by `unique_id` (never title text), then re-checked every other title-text edit from this entire session for the same collision class - all came back clean, confirming this bug was isolated to just these two rows. This is exactly the danger DEFINITIONS.md's own "Remakes" note already warns about (two same-named titles aren't necessarily related) - a real lesson to carry forward: any future batch edit targeting rows by title text needs a same-titled-but-unrelated check first, same as the Star Trek TOS volume-numbering work already matches by `unique_id` once real candidates are identified.

Final state, verified directly against Supabase: `Batman and Robin` (1949, tt0041162), `Flash Gordon Conquers the Universe`, and both `Zorro Rides Again` rows are `Serial`/season_no "1" with their existing episode_count/runtime untouched; `Dick Tracy` (1937, tt0028785) is `Serial`/season_no "1" but its `episode_count` is deliberately left null (unlike the other three, this disc has no season/episode data at all, just a single 70-min entry - needs the user to check the physical case for the real chapter count before it can be filled in); `Batman and Robin` (1997)/`Dick Tracy` (1945) are back to plain `Movie`/season_no null, exactly as before. Wired "Serial" into `ConfirmScreen.tsx`'s `SEASON_FIELDS_MOVIE_OR_TV_VALUES` (season/episode fields now show for it) and added a small effect auto-filling `season_no` to "1" the moment "Serial" is picked (same "don't stomp a manual entry" guard the disk-region default already uses) - `tsc --noEmit` clean on `apps/mobile`. Documented the full rule, both bugs, and the deferred nested-serial case in `Claude/RESOURCES.md`'s SERIALS note and a new bullet in `barcode-review-screen-fields.md`'s TV Scanning section.

Not yet done: the user's episode_count for `Dick Tracy` (1937) (needs the physical disc checked), the deferred nested-serial-in-a-show logic (explicitly not needed yet per the user), and pushing any of this session's work to either GitHub repo.

2026-09-18 (same day, follow-up): User confirmed episode_count for `Dick Tracy` (1937) should stay untouched - only the physical case can say how many chapters are actually on that disc, and asked for a related, more general feature: when scanning/confirming a Serial (or any TV-ish type), the Episode Count field should be visible so the user can type in the real count themselves from the case. Confirmed this was already true (already built into the `SEASON_FIELDS_MOVIE_OR_TV_VALUES`-gated block from the previous entry) rather than building anything new.

User then asked two more things, both live/proactive rather than reactive to a bug: (1) whether any check exists to block a scan whose barcode returns non-DVD/physical-media product data - checked the actual pipeline (`packages/shared/src/upc.ts`'s `upcLookup`) and confirmed none exists; UPCitemdb's real API response does carry a `category` field, but it was being discarded entirely. (2) After their first live TV-series scan test, realized that typing just the show's name during manual title search would catalogue the bare show name with no season info at all, since OMDb only has one entry per whole show - asked for a title-composition workflow.

Built both. For the category check: kept UPCitemdb's `category` field on `UpcProduct`, and added `looksLikeNonMediaCategory` (an allowlist of media keywords - movie/film/TV/DVD/Blu-ray/etc. - rather than a denylist of "bad" categories) - deliberately soft/warn-don't-block, verified against a live test against a real DVD already in the collection ("Blade Runner The Final Cut") that UPCitemdb itself files under `"Electronics > ... > DVD & Blu-ray Players"`, not "Movies" - proof that a hard block on this data would have rejected a real, correct scan. `ConfirmScreen.tsx` now shows an amber warning banner (same style as `OfflineBanner.tsx`) naming the actual category when it doesn't look like media, but never disables Confirm.

For the title/season composition: `ConfirmScreen.tsx`'s Title field (previously only ever shown for a fully-manual entry, `selected.size === 0`) now also shows whenever exactly one real candidate is selected, and auto-suggests `"<Show> Season <N>"` (plus `" Part <N>"` when set) via two effects - a hard reset the moment the selected candidate changes, a soft update (backs off once the field no longer matches the tool's own last suggestion) as Season No./Part of a Season No. are typed afterward - always fully editable, since the collection's own real titles already use several different phrasings for this ("Season N", "Series N", "the Complete Nth Season", a bare number with no word). The composed value is now sent as a `title` override on the single-candidate path too (previously `manual.title` was only ever sent on the fully-manual path, so a matched TV candidate had no way to override OMDb's bare title at all). Also fixed a stale doc reference along the way: `barcode-scanning-pipeline.md` still said the two dev servers connect over port 3000 - corrected to 3001 to match the port mismatch found and fixed earlier this session.

`tsc --noEmit` clean across `shared`/`backend`/`web`/`mobile`/`scripts`. Documented both features in `barcode-scanning-pipeline.md` (NON-MEDIA BARCODE WARNING, TV TITLE + SEASON/PART COMPOSITION notes). Confirmed `tmdb_media_type` (0028) stays database-only, no Sheet column - matches the existing `tmdb_id`/`tmdb_synced_at` precedent (only `tmdb_page`, the clickable link, is in `AUTO_CREATE_COLUMNS`).

Not yet tested live on a real device - both features typecheck but the user was mid-test when these were built; still owed: watching for how these actually behave once tried for real, `Dick Tracy` (1937)'s episode_count, the deferred nested-serial logic, and pushing this session's work to either GitHub repo.

2026-09-18 (same day, follow-up): User gave three pieces of feedback on the Type section after trying it live: move it from near the bottom of the form to right after the best-match/candidate picker (so it's settled before the Title suggestion below it, which can then account for it); make the guess itself reactive to whichever candidate is the current best match, not frozen at mount; and swap the actual control from a row of chips to a genuine dropdown, since that wasn't what they meant by "dropdown box with no text entry" earlier in the session.

Built a new `SelectDropdown.tsx` (tap the field, a modal list opens, tap an option, it closes) and swapped it in for the Movie/TV field specifically - `SingleSelectChips` (the chip-row component) is untouched and still used for Disc Condition, a genuinely different shape of control. Moved the whole Type block (dropdown + Season No./Part of a Season No./Episode Count) from near the Confirm button up to right after the candidate list. Made the guess reactive: removed the one-time guess from `movieOrTv`'s `useState` initializer and replaced it with a `useEffect` keyed on `singleSelectedImdbId` that re-guesses every time the selected candidate changes, using the same "back off once the field no longer holds the tool's own last suggestion" guard the Title composition effects already use, and never running at all when a draft or an already-catalogued rescan value exists. `tsc --noEmit` clean on `apps/mobile`. Documented all three changes together in `barcode-review-screen-fields.md`'s TV Scanning section.

Not yet done: `Dick Tracy` (1937)'s episode_count, the deferred nested-serial logic, live device testing of everything built this session, and pushing to either GitHub repo.

2026-09-19: User's first real live TV scan (Doctor Who Season 21) surfaced four real bugs plus two feature requests in one message. Investigated and fixed all six:

1. **Running Time was wrongly auto-filled for a TV Series.** `/api/scan/confirm/route.ts` set `running_time_mins` from OMDb's `Runtime` unconditionally, never actually gated on `movie_or_tv` the way RESOURCES.md's own rule says it should be - the n/a-by-default TV Series rule was only ever enforced for a fully-manual entry's fields, never for a real OMDb-matched candidate. Fixed by resolving the final `movie_or_tv` (manual override, else OMDb's own guess) before deciding whether to keep the OMDb runtime, nulling it out only for `"TV Series"`.
2. **A TV title's poster resolved to a completely unrelated film's** (Doctor Who Season 21 showed The Lord of the Rings: The Two Towers on Estimated Value). Root cause: `resolveTmdbCandidateById` (used by `resolvePosterUrl`) always called `/movie/{id}` regardless of media type - movie and TV ids are separate TMDb numbering spaces, so a TV id read as a movie id can coincidentally be a real unrelated movie. Fixed by threading the title's own `tmdb_media_type` through, branching to `/tv/{id}?append_to_response=external_ids` when appropriate.
3. **Genre "Adventure, Drama, Family" instead of Sci-Fi** - checked directly against OMDb (`tt0056751`, the 1963 series) and confirmed this is genuinely OMDb's own data for that entry, not a bug (the 2005 revival, `tt0436992`, does say Sci-Fi). Noted as a real gap though: there's currently no way to manually override Genre on a real OMDb-matched candidate at all, same restriction as Running Time.
4. **Candidate poster list scrolling was laggy** - a plain `ScrollView` mounted and decoded every candidate's poster image up front, including off-screen ones, and remote posters are usually much higher-resolution than their 120x168 display size. Swapped both candidate rows (main + Extras) to a horizontal `FlatList`, which virtualizes off-screen cells - no new dependency needed.
5. **Replaced `AutocompleteInput`/`TagAutocompleteInput` with new `SearchableModalInput`/`TagSearchableModalInput`** everywhere they were used (Format, Genre Location, Rating, Studio, Original Language, Rented By Who, Animation/Live Action, Genre, Franchise) - the user liked the new Type `SelectDropdown`'s full-screen feel and asked for the same treatment generally, calling out Genre/Franchise specifically. Same props/matching logic as before, but tapping the field now opens a full-screen modal with a live-filtering text input at the top instead of an inline dropdown; tapping an option fills the text without auto-closing (so several tags can be picked in one sitting), and a Done button or hiding the keyboard closes it. Deleted the two old components outright rather than leaving them unused.
6. **Built a UPC-lookup-quota progress bar for PendingScansScreen**, mirroring the existing Amazon-quota bar pattern: new `upc_provider_quota` table (`0029_add_upc_provider_quota.sql`, day-keyed, RLS enabled from the start this time), `upcQuota.ts`, wired into the one real call site of `upcLookup` (`resolvePendingScansBatch`). Unlike the Amazon quota, this is a core feature in both public and private builds.

`tsc --noEmit` clean across `backend`/`web`/`mobile`. Documented all six in `barcode-review-screen-fields.md`'s TV Scanning section. Still owed: applying migration 0029 to both Supabase projects (needs a fresh token from the user), a manual fix/rescan for the already-wrong Doctor Who Season 21 row (the runtime/poster fixes are forward-only), `Dick Tracy` (1937)'s episode_count, the deferred nested-serial logic, live device testing, and pushing to either GitHub repo.

2026-09-19 (same day, follow-up): user gave a fresh Supabase token and asked three more things in quick succession.

1. **"Does TMDb use genre data? Merge OMDb + TMDb genre so we get all the genre information"** - applied migration 0029 with the new token first. Before merging, queried the real collection's distinct genre values live (`select distinct unnest(genre)`) to check for naming conflicts - found the Genre column already carries substantial manual curation well beyond raw OMDb text (compound genres like "Sword and Sandal"/"Kaiju"/"Wuxia" no API ever supplies, plus real typos/casing drift), confirmed the user hadn't noticed this scope creep, and asked two clarifying questions before implementing: map TMDb's "Science Fiction" to the collection's own established "Sci-Fi", and exclude TMDb's "TV Movie" genre tag (not a real content genre) from the merge. Both confirmed. Built `mergeOmdbAndTmdbGenres` in the confirm route, plumbed `genres: string[]` through `fetchTmdbFieldsById`/`TmdbFields`/`lookupTmdbFields`.
2. The user's own follow-up on the "TV Movie" exclusion: "if a movie is given the TV Movie tag, maybe movie_or_tv should reflect that - but check you're not looking at the wrong Doctor Who first." Verified live rather than assumed: looked up both the classic show (`tt0056751` -> TMDb tv/121, genres include "Sci-Fi & Fantasy") and the actual 1996 TV movie (`tt0116118` -> TMDb movie/15691, genres include "TV Movie") directly - confirmed TMDb's `/find` always resolves via the exact candidate's own imdbID, so there's no cross-contamination risk, and "TV Movie" only ever appears on TMDb's *movie* genre list, never its TV one. Used this to sharpen (never force) the movie_or_tv guess: a second reactive effect in ConfirmScreen bumps "Movie" to "TV Movie" once the TMDb preview arrives with that tag, same backoff-once-edited guard as every other reactive guess on the screen.
3. **UPC quota bug report**: "why does the progress bar say 0/100 when I scanned 3+ titles today?" - root cause was inherent to the self-tracked-counter design shipped minutes earlier: the table didn't exist until this session, so it could never know about earlier calls. Checked UPCitemdb's actual response headers live (`curl -D -`) rather than assuming, and found it genuinely exposes real `X-RateLimit-*` headers (unlike the RapidAPI Amazon providers, confirmed NOT to have this). Redesigned the whole feature same-day: `upcLookup` now returns `{ product, rateLimit }`, and a single-row `upc_quota_status` table just mirrors UPCitemdb's own authoritative snapshot on every call instead of counting anything itself - immediately accurate, including for the day's earlier calls, the moment the next real lookup happens. Superseded migration 0029's original day-keyed-counter shape in place (safe - it had zero real rows) and seeded the new table with a real live snapshot so the bar reads correctly right away rather than waiting on the next scan.

Also, mid-turn: **"the Title field shouldn't be visible unless it's genuinely being modified - like a collection or TV entry - hide it for a plain movie with a linked OMDB/TMDb title."** Changed the Title field's visibility from `!isMultiTitleCollection` alone to also require `selected.size === 0 || showSeasonFields || isCollection` - hidden now for an ordinary single-movie match, still shown for fully-manual entries, TV-ish types (season/part composition), and collection/box-set scans. Confirmed safe by construction: the existing season-composition hard-reset effect keeps `manualTitle` synced to the real candidate title regardless of whether the field renders, so a hidden field's title override still exactly matches OMDb's own title at submit time.

`tsc --noEmit` clean across all four packages after every change. Documented all four in `barcode-review-screen-fields.md`'s TV Scanning section. Migration 0029 (final shape) applied and verified live on both Supabase projects.

2026-09-19 (same day, second follow-up): user reported a real scan (Resident Evil: Extinction) came back with no best-match candidate at all and missed auto-fillable detail, and asked me to rework the UPC listing-text parsing rather than guess - "that text has inconsistent formatting and different types of information every time... you might have to rework your parsing." Said they'd give hints on future scans if something's still missed.

Found the actual stuck row directly in Supabase rather than asking the user to describe it: `pending_scans` had the real UPCitemdb listing title `"Resident Evil: Extinction [regions 2,5] - Dvd - - Free Shipping."`, resolved with 0 OMDB candidates. Traced the root cause by querying OMDB directly with both the actual garbled search query the code was producing and a manually-cleaned version side by side - the garbled one genuinely returned "Movie not found!", confirming this wasn't a red herring. Root cause: `cleanProductTitleForSearch` never stripped square-bracketed listing metadata at all (only round-bracketed), and its region-word pattern only matched the singular "region" plus one token, never "regions" plus a comma-list - so `"[regions 2,5]"` survived intact and broke the search. Fixed both gaps, and tested the fix against multiple real historical listings (Paper Planes, Captain Marvel, Blade Runner) plus synthetic edge cases before trusting it, specifically to catch a regression risk an early draft had (a broadened region-list capture that would have also swallowed an unrelated adjacent digit, e.g. mistaking part of "2015" for a region code).

Also built real auto-fill from this, not just the bug fix, per the user's actual framing ("find what information can be taken from that string ... used to auto fill the form"): `extractDiskRegionHint` (`packages/shared/src/formatHints.ts`) now pulls real disc-region codes straight out of listing text ("[regions 2,5]" -> "2, 5", "Region Free" -> "All") and pre-fills ConfirmScreen's Disk Region field - a field that was always left for the user to fill in by hand even when the listing already stated it. Deliberately conservative (word-bounded single-character tokens only, restricted to codes valid for the resolved format) to avoid the same year-digit false-positive risk.

Verified the actual fix end-to-end, not just via isolated regex tests: reset the real stuck pending_scans row back to "pending" and re-triggered `/api/scan/resolve` against the live dev server twice. First attempt still returned 0 candidates despite the code being correct (confirmed by running the real, unmodified functions directly via `tsx`, bypassing Next.js) - traced to the long-running `next dev` process not having picked up the `packages/shared` change. Restarted it (including clearing the same orphaned-port-process gotcha from earlier in the session) and re-resolved: real 4 OMDB candidates came back this time, best match "Resident Evil: Extinction", exactly right.

`tsc --noEmit` clean across all four packages. Documented the whole investigation (root cause, the avoided regression, the dev-server-restart gotcha) in `barcode-scanning-pipeline.md`'s new UPC LISTING TEXT PARSING section.

2026-09-20: two more requests in one message.

1. "The Resident Evil: Extinction scan I just confirmed got genre_location 'Video Game' by accident instead of 'Video Game Movie' - remove 'Video Game' from the dropdown and fix that row." Queried Supabase directly first rather than assuming: confirmed "Video Game" was used by exactly that one row (and "Video Game Movie" already existed as the real established category on one other row), so fixing the one row is the whole fix - the genre_location dropdown is a live-distinct-values list (SearchableModalInput/fieldOptions.ts), not a fixed enum, so there's no separate "remove from options" step once no row uses the wrong value any more. Wrote `scripts/src/backfill-resident-evil-genre-location.ts` (dry-run by default, same convention as backfill-commando-rental.ts, refuses to overwrite if the row's value isn't exactly what was expected), ran it with `--apply` - updated both Supabase and the Sheet.
2. "Are any non-optional pending-scan fields left un-warned/un-blocked when empty?" - checked and confirmed no, they weren't: a blank Title/Movie-or-TV/Format all silently fell through to a placeholder default ("Unknown Title"/"Movie"/"DVD") in the confirm route rather than actually stopping the submission. Built `getMissingRequiredFields()` in ConfirmScreen.tsx, checked at the top of `handleConfirmPressed` (the one choke point every Confirm-button path already goes through) - blocks submission and lists every currently-blank required field at once. Asked the user directly whether Rating/Studio/Franchise/Original Language (unlabeled but only shown once TMDb comes up empty) should count as required too, since forcing an answer isn't right for genuinely case-doesn't-say situations - the user's own reasoning was that once the program is asking the user to manually supply something OMDb/TMDb couldn't, that IS the one chance to capture it, so yes. Implemented Rating/Studio/Original Language as required-when-shown accordingly, but deliberately left Franchise out of that same bucket despite the question grouping it in - a blank Franchise is very often the genuinely correct answer (most films aren't part of one), unlike the other three, so requiring it would wrongly block any standalone film. Flagged this judgment call back to the user rather than silently including or excluding it.

`tsc --noEmit` clean across all four packages after both changes. Documented the required-field validation in `barcode-review-screen-fields.md`'s new REQUIRED-FIELD VALIDATION section.

2026-09-20 (same day, later): User asked to plan and build Collection integration for the scanner app - the schema/confirm-route support had existed since early Phase 1, but the actual scanning flow for creating a collection was only ever partially built (a keyword-guessed `isCollection` flag and a checklist UI that read from a near-always-empty OMDB search of the box's own listing title). Read through every relevant planning doc and the live code first, then asked clarifying questions and got four real decisions: drop the original franchise/director/actor-name OMDB-search idea entirely in favor of a manual per-title entry loop; add a shared physical-fields step (Format, Disk Region, etc.) filled in once for the whole box before that loop starts; Name of Collection as a plain always-editable free-text field, pre-filled from the listing title; and a manual toggle to mark/unmark a scan as a collection, independent of the automatic keyword guess. A design agent then drafted the concrete implementation plan, which surfaced three more real questions, each resolved directly with the user: Genre Location should be a fully **hard-shared** field, not just a shared default - "you can't get an axe and chop a box set into multiple parts" to put them on different shelves; the collection header's own `movie_or_tv` should stay a normal editable field defaulting to the existing plain fallback, since some collections are all-movie and some are all-TV; and Watched/Watched Disc needed a real per-member + header-rollup design rather than being hidden - each title in a collection gets its own Watched/Watched Disc, and "Watched Collection"/"Watched Collection Disc" (the header row's own existing `watched`/`watched_disc` columns, no new schema needed) auto-derive once every member agrees, or cascade down to every member when toggled by hand. Duplicate detection for collections turned out to be a real, scoped feature too, not just a warning: the user wants the existing Overwrite/Is-a-new-entry/Reject choice to fire for a collection or collection-member match, with the matched entry's own collection membership shown for context - but explicitly *never* comparing a collection/member against a standalone title, since owning a film both standalone and inside a box set is legitimate.

Built end to end: `scanResolver.ts` now skips the OMDB search entirely once `looksLikeCollection` flags a listing, rather than running a search that almost never matches a real film. `apps/web/src/app/api/scan/find-existing/route.ts` gained a `scopeToCollections` flag (filters candidates to `is_collection || title_in_a_collection` before matching) and now returns `is_collection`/`title_in_a_collection`/`name_of_collection` on every candidate. `apps/web/src/app/api/scan/confirm/route.ts`'s `overwriteUniqueId` moved from a single request-level field to a per-entry one (`ConfirmEntry.overwriteUniqueId`) - found necessary once "Overwrite" needed to apply to one specific entry within a multi-entry collection submission rather than the whole request; updated every call site across `scanApi.ts`, `offlineQueue.ts`, and `OfflineQueueScreen.tsx` to match. Built a new standalone `apps/mobile/src/components/TitleSearchPicker.tsx` for the per-title add loop, deliberately not a reuse of ConfirmScreen's own inline single-title search block (too entangled with logic that doesn't apply to a collection member). `ConfirmScreen.tsx` got a manual collection toggle (`isCollectionOverride`, persisted in `confirmDrafts.ts`), an entirely separate render branch for collection mode reusing existing single-title state where the meaning genuinely carries over (`manualTitle` doubles as Name of Collection, `movieOrTv` as the header's own type, `watched`/`watchedDisc` as Watched Collection/Watched Collection Disc) rather than declaring parallel state, a header-level duplicate-check screen mirroring the existing one, and `performCreateCollection`/`handleConfirmCollectionPressed`. Along the way, found and fixed a real latent bug the old collection checklist would have had if it had ever actually populated: the single shared `manualFields` object sent to every entry in a multi-select submission would have force-labeled every member of a mixed movie/TV-episode collection with whichever `movie_or_tv` was picked once for the whole set - the new per-entry `manualFields` construction for collections avoids this by design. Also simplified `toggleCandidate`/removed the now-dead `isMultiTitleCollection` (provably always false once collections stopped reusing the ordinary candidate multi-select) from the single-title path, since collections no longer touch it at all.

`tsc --noEmit` clean across all five workspaces (mobile, web, shared, backend, scripts) after every change. Documented the finished design in `barcode-scanning-pipeline.md`'s Collections section, replacing the superseded franchise-search description (kept for context, clearly marked as superseded rather than deleted). Not yet verified against a real live scan on-device - next session should do a real end-to-end test (toggle on, shared fields, a few real titles via the add loop, watched-cascade, the duplicate check both ways) per the plan's own verification section, then delete the test rows immediately after per the project's standing practice.

2026-09-20 (same day, follow-up): user started real live testing on their phone and reported two real issues.

1. **"Watched Collection Disc selects Watched Collection too, then immediately deselects both."** Root cause, found by re-reading the bottom-up auto-derive `useEffect`s added in the previous session: they computed the header's checked state as `collectionMembers.length > 0 && collectionMembers.every(...)`, so with zero titles added yet, that expression is `false` - meaning the instant a manual toggle cascaded `true` onto the (empty) member list, the same effect re-ran on that empty-array change and read "no members" as "not everything's watched," forcing both checkboxes straight back off. Fixed by skipping both derive effects entirely while `collectionMembers.length === 0` - with nothing to derive a consensus from, the header checkbox is left exactly as the user set it, and real derivation only takes over once a first title actually exists to agree or disagree with it.
2. **"Each title in the collection I'm logging has its own 4K UHD disc and its own Blu-ray special-features disc - does the format setup interfere with that?"** It did: Format/Disc Count/Special Features had been made hard-shared across the whole submission the previous session (one value applied to the header row *and* every title), on the reasoning that "a box set is one physical object" - true for shelf location, but wrong here, since each title in a box set typically has its own separate disc(s) that can differ from the others. Asked the user directly whether to move these to per-title fields, and separately whether the header's own Disc Count should auto-total rather than be typed - confirmed both, with one addition: "a collection can have a special features disk for the whole set and a title can have a special features disk for that particular title," i.e. both levels need to coexist, not one replacing the other.

   Redesigned accordingly: the header/shared step keeps its own Format (the box's general format) and Special Features (+ sub-fields, now relabeled to mean the *collection's own* bonus disc, gated on the toggle alone rather than the old "more than 1 disc" check, which doesn't apply to a header with no single primary disc of its own) - its Disc Count input was removed entirely, replaced by a live-computed read-only total (`collectionTotalDiscCount` = every title's own Disc Count summed, plus the collection's own bonus disc count if it has one). `TitleSearchPicker.tsx` gained its own Format/Disc Count/Special Features (+ sub-fields) for each title, seeded via new `initial*` props from the previously-added title's own values (or the header's shared-step values for the very first title) - cuts down on re-typing the same pattern for a box set where most/all titles share it, while still letting any individual title differ. `CollectionMember`/`ConfirmDraftCollectionMember` gained the four new fields; `performCreateCollection` now builds the header's own format/disc_count/special_features from the computed total and shared-step state, and each member's from its own fields, instead of spreading one shared object into both. Also found and fixed a real pre-existing gap while touching `buildSharedCollectionFields` for this: `release_name`/`releaseNameMatchesTitle` had a real input on the shared-fields step but was never actually included in the object returned to the confirm route at all, silently dropping whatever was typed there on every collection submission since the feature shipped a session ago - added it to the shared bucket.

`tsc --noEmit` clean across `apps/mobile` (and re-confirmed clean on `apps/web` too, unaffected by this pass) after both fixes. Updated `barcode-scanning-pipeline.md`'s Collections section to describe the revised per-title-plus-collection-level disc fields in place of the original hard-shared description.

2026-09-20 (same day, still later): user reported the Collection flow's longer field labels ("Special Features (a bonus disc for the whole collection)" etc.) were pushing their own Switch/checkbox clean off the right edge of the screen, and asked to apply the "graphics safe area" principles discussed early in the project - which turned out not to be written down anywhere in `Claude/`, only recovered by asking the user to re-explain it. Their answer: the app targets Android specifically, so no element should sit too close to the top (the front-camera punch-hole) or bottom (the on-screen navigation buttons) of the screen.

Checked first, rather than assuming a gap: the vertical half of that principle is already correctly implemented everywhere via `react-native-safe-area-context`'s `useSafeAreaInsets()` - no actual bug there. The real bug is on a different axis entirely: React Native's flexbox defaults every child to `flexShrink: 0`, so a long `Text` label next to a fixed-size Switch/checkbox in a `row` (`justifyContent: "space-between"`) renders at its own full natural width instead of wrapping, pushing the control off-screen once the label is long enough - exactly what the Collection flow's newer, longer labels started triggering. Fixed by adding `flexShrink: 1` to every label/body text style used beside a control (`label`, `checkboxLabel`, `body` in both `ConfirmScreen.tsx` and `TitleSearchPicker.tsx`) plus a `gap` on the shared `row` style, so a long label now wraps onto multiple lines instead of pushing its control past the visible screen. `tsc --noEmit` clean on `apps/mobile` afterward. Documented both halves of the safe-area principle (the already-correct vertical one and the newly-fixed horizontal one) in `WEB APP DESIGN.md`'s new MOBILE LAYOUT SAFE AREAS section, specifically so this doesn't get lost a second time.

2026-09-20 (same day, still later): user gave four more pieces of feedback on the Collection flow in one message, all implemented.

1. **"Move the collection section to the top of the form."** The Name of Collection/Collection Type/Watched Collection block used to sit after the shared physical-fields step; moved to right after the "This is a collection" toggle, before Shared Details - settling what the collection actually is before asking about its discs.
2. **"If a user selects that they've seen all the disks in this collection, Watched/Watched Disc shouldn't need to show up when logging titles."** `TitleSearchPicker` now takes `impliedWatched`/`impliedWatchedDisc` props (ConfirmScreen's own current `watched`/`watchedDisc` header state) and seeds its own Watched/Watched Disc state from them - when Watched Collection Disc is already checked, both checkboxes are hidden entirely from the add-a-title form (defaulting to true); when only Watched Collection is checked, only "Watched" hides (Watched Disc stays a real, visible question, since watching the film doesn't necessarily mean via this specific disc). Either can still be corrected per-title afterward from the running member list.
3. **"Only show Overwrite/Is-a-new-entry for the whole collection once all titles are added, not per individual title."** This was the biggest change: the per-title duplicate check that ran inside `TitleSearchPicker` at add time (added the previous session) was removed entirely - adding a title now always just adds it, no interruption. `handleConfirmCollectionPressed` (ConfirmScreen.tsx) now runs the collection-scoped duplicate check for the header AND every member at once (`Promise.all`) right before submission, builds a queue of only the entries that actually came back with a match, and presents them one at a time (a new numbered "N of M" resolution screen, reusing the existing poster/field-comparison layout) if the queue is non-empty - an empty queue (no duplicates anywhere) skips straight to submission. `performCreateCollection` changed from a single optional `headerOverwriteId` parameter to an `overwriteDecisions: Record<string, string | undefined>` map (keyed "header" or a member's own `key`), built by the resolution screen as each queue item is resolved.
4. **"Only submit to the database once the collection and all its titles have the right details - one action for everything."** Checked rather than assumed: this was already true - `TitleSearchPicker`'s `onAdd` only ever updates local `collectionMembers` state, no network write happens until `performCreateCollection` sends the header + every member in one `confirmScan` call. The only network activity that used to happen mid-add-loop (the per-title duplicate check, a read) was removed by point 3 above anyway, so there's now zero network activity at all until Confirm.

`tsc --noEmit` clean on `apps/mobile` after all four changes. Updated `barcode-scanning-pipeline.md`'s Collections section to describe the reordered form, the implied-watched hiding, the deferred collection-wide duplicate queue, and the single-submission guarantee.

2026-09-20 (same day, final and largest pass): user reported the real, already-scanned "Alfred Hitchcock Classics" collection had 19 separate problems, split explicitly into "fix the real data" and "fix the algorithm so this can't happen to future collections" - a mix of real production-data repair and a large batch of new collection-scanning features.

**Investigation first, not guessing**: queried the real Supabase data directly before touching anything. Found the 4 new member rows (Rear Window, Psycho, The Birds, Vertigo) still had their real Letterboxd-derived `personal_rating`/`watched`/`last_watched_date` intact - reading `/api/scan/confirm` directly confirmed why: those two fields are never part of any entry's own write payload at all (only `personal_rating`/`last_watched_date` - `watched`/`watched_disc` ARE sent every time and were genuinely at risk). Also found the real root cause of the header failing to overwrite an old pre-barcode legacy row ("The Alfred Hitchcock Classics Collection 4K"): exact-title-text matching, and no shared barcode (the legacy row predates barcode scanning) - not a mysterious bug, a real limitation of the matching approach. And found the user's own "Studio should be Universal" expectation didn't match any of the 4 real TMDb-resolved production companies at all (Paramount, Shamley Productions, Alfred J. Hitchcock Productions) - Universal is almost certainly the disc's real-world distributor, a different fact TMDb doesn't expose; flagged this back to the user rather than silently forcing "Universal" in.

For the two wrong NZ ratings (Psycho was "R", The Birds was "PG-13" - neither a real NZ code), checked `packages/backend/src/tmdb.ts` directly first: it already correctly filters to `iso_3166_1 === "NZ"` specifically, so this isn't a "falls back to US rating" bug - TMDb's own NZ certification data for these two films is itself wrong/crowdsourced-bad. Searched for an authoritative NZ source before asking the user to just supply the answer (per their own explicit condition - only ask if there's genuinely no way to look it up): found New Zealand's real Film & Video Labelling Body public classification bulletin (fvlb.org.nz) via web search, fetched the actual November 2020 PDF, and found real entries for Psycho (M) and The Birds (PG) approved the same day as several other Hitchcock Blu-ray titles - almost certainly the same release batch. The user then corrected this live with their own physical discs: Psycho is actually R13, The Birds is actually M - used their correction as ground truth over the public database (a different edition/reclassification is plausible).

Built, in order:
- **Overwrite protection for `watched`/`watched_disc`, universal** (`/api/scan/confirm`) - fetches the existing row's own values before an Overwrite and ORs them into the final write, so an Overwrite can only ever add watched status, never remove it.
- **NZ rating validation** (`packages/backend/src/tmdb.ts`) - a real `VALID_NZ_RATINGS` set (G/PG/M/R12/R13/R15/R16/R18); TMDb's own NZ certification is only trusted if it's actually one of these, else treated as no answer at all (reveals the manual field instead of writing garbage).
- **Collection header aggregation** (`/api/scan/confirm`, a real restructuring into build-all-entries-first / aggregate-header / write-all-entries three phases, so the header can read every member's own final resolved values before any writes happen): Genre/Franchise as a union across members, Director as the most-common single value, Studio as most-common-among-members else a new manual header "Distributor" field else "n/a", Special Features derived true if any member has one, Disc Count as the real summed total, Personal Rating as the average when every member is watched+rated, Last Watched Date as the latest among members. Removed the old `isMultiTitleEntry` guard that used to blank manual Rating/Studio/Original Language on any multi-entry submission - a leftover from the old shared-manualFields design that was incorrectly also suppressing the header's own new manual Rating/Distributor fields.
- **Rating field added to the collection header** (ConfirmScreen.tsx) - always visible, required, since there's no TMDb id for a header to auto-fill it from.
- **Per-title Release Name** (`TitleSearchPicker.tsx`) - moved out of the shared bucket per the user's own point about a definitive/remastered edition of one film being bundled into an otherwise-ordinary set.
- **Every member shares the header's own barcode** and **gets `number_of_titles_in_collection` set to match the header** - both simple client-side additions to `performCreateCollection`.
- **Genre Location dropdown filtered by media type** (`filterGenreLocationOptions`, ConfirmScreen.tsx) - the user has started prefixing shelf-location names ("TV " / "COLLECTION " / no prefix) to disambiguate same-named shelves across formats; the dropdown now only shows the locations that apply to whatever's currently being catalogued, for both the collection flow and the ordinary single-title flow.
- **Collection-wide duplicate matching redesigned again** (`/api/scan/find-existing`) - the single-decision flow from earlier the same day still matched the header by exact title text, which is exactly what failed for the real legacy row. Replaced with a genuine fuzzy match (`textSimilarity`, a local Dice-coefficient word-overlap scorer - deliberately not shared from `packages/backend/src/estimatedValue/confidence.ts`'s near-identical one, since that whole module is stripped from the public build and collection matching has to work there too) combining name similarity, a format-match bonus, and - the strongest signal - how many of the new collection's own member titles exactly match an existing member already catalogued under a candidate collection. The single Overwrite decision now resolves both the header AND every member's own overwrite target in one pass, using the matched candidate's own `existingMemberTitles` (returned alongside it) - a member with no exact match among the candidate's existing titles is simply added as new, never blocked.
- **Deliberately deferred, disclosed rather than skipped**: per-member Studio still uses TMDb's own single first-listed production company, unchanged - the user's fuller idea (each member's studio should prefer whichever company is most common across every member's *entire* TMDb company list, not just each film's own first-listed pick) needs `TmdbFields` extended to carry each film's full company list plus a real cross-referencing pass, logged as a real follow-up rather than attempted in the same already-large session.

Verified estimated_value's existing behavior for collection members (item raised twice by the user) needed no code change at all - already null in the DB (never part of the write payload, and `refreshEstimatedValueForTitleIds` already skips `title_in_a_collection` rows) and already renders as literal "n/a" in the Sheet (the codebase's own universal empty-cell convention, `formatValueForSheet`).

Wrote `scripts/src/backfill-hitchcock-collection-fix.ts` (dry-run by default, matches the project's established one-time-backfill convention) to repair the real Hitchcock rows: the header's aggregated fields (computed by hand to match what the now-fixed algorithm would produce), each member's barcode/number_of_titles_in_collection/release_name, the two real corrected ratings, and deleting the old legacy stub row entirely (the new header row is a strict superset of its data, and the duplicate only exists because of the exact-matching gap now fixed for future scans). Dry-ran first, confirmed the expected current state exactly, asked the user directly before running for real given it deletes a row - approved, ran with `--apply`, then independently re-queried the live data afterward (a separate throwaway script, deleted immediately after) to confirm all 6 changes actually landed: 24 Hitchcock-related rows now (was 25, legacy stub gone), header carrying every aggregated field correctly, all 4 members sharing the real barcode/title count, Psycho and The Birds showing R13/M, and every member's own Letterboxd-derived personal_rating/last_watched_date still intact throughout.

`tsc --noEmit` clean across all five workspaces (mobile, web, shared, backend, scripts) after every change in this pass.

